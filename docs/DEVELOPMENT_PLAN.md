# SuZu One — Development Plan

| | |
|---|---|
| **Version** | 0.3 (Phase 10 — projects & daily work management — added) |
| **Date** | 2026-09-22 |
| **Companion doc** | [SRS.md](./SRS.md) — requirement IDs below (FR-…, NFR-…) refer to it |

---

## 1. Delivery strategy

Four principles drive the order of work:

1. **Ship something people use every day, early.** The first live milestone comes about 6 weeks in (employee records + directory), then check-in and leave. Daily use builds the habit and the data that later modules depend on.
2. **Build the two shared engines once.** The *approval engine* and the *task engine* (SRS §3.1) are platform work. Leave, OT, requests, payroll sign-off, onboarding checklists, work management and the compliance tracker all reuse them.
3. **Payroll comes after the data is trustworthy.** Payroll needs at least two clean months of locked timesheets and leave data, then two months of parallel run. So it is built mid-plan, while attendance data accumulates — and work management (which the leaders want now) ships before it.
4. **Roll out by pilot.** Each module goes to one pilot entity or department first, behind a feature flag, then to the whole group.

### Phase order at a glance

| Phase | Scope | Indicative duration | Live milestone |
|---|---|---|---|
| **0** | Foundation & platform | 3 wks | — (internal) |
| **1** | Core HR | 4 wks | **M1** — HR spreadsheets retired; everyone can sign in, directory and org chart live |
| **2** | Attendance & leave | 5 wks | **M2** — daily check-in, leave and OT requests, monthly timesheet lock |
| **3** | Work management + Operations & compliance tracker | 5 wks | **M3** — leaders assign and track tasks; HR/finance deadline calendar live |
| **3.5** | OKRs & KPIs (moved forward from Phase 8) | 2 wks | **M3.5** — 2027 goals and KPIs tracked in-system from Q1 2027 |
| **4** | Knowledge base + internal comms | 3 wks | **M4** — policies published and acknowledged; home feed |
| **5** | Payroll | 6 wks build + 2 months parallel run | **M5** — first official payroll from the system |
| **6** | Assets & requests | 3 wks | **M6** — paper/chat approvals retired |
| **7** | Recruitment (ATS) | 4 wks | **M7** — careers page live, candidate → employee flow |
| **8** | Performance reviews + performance-driven year-end bonus | 3 wks | **M8** — review cycle in-system; 2027 year-end bonus computed from KPI/OKR results |
| **9** | AI assistant + analytics | 3 wks | **M9** — "Ask SuZu" and owner dashboard v2 |
| **10** | Projects & daily work management (PJM) — six releases | 12 wks | **M10** — every team plans, executes, hands off and delivers in the app; old trackers retired (SRS D21) |
| **11** | CRM | separate SRS | — |

Build time for the full HRM suite (phases 0–9) is roughly **41 weeks ≈ 9–10 months**. Starting late September 2026, that puts M1 in early November 2026, M2 in mid-December 2026, M3 in late January 2027, M3.5 in February 2027 and the first official payroll (M5) around mid-2027. The payroll parallel run overlaps phases 6–7.

> **About the estimates.** The team is the owner plus Claude Code (SRS D9). Durations assume steady, near-full-time build sessions, plus part-time input from the HR lead and the chief accountant for UAT. If the owner's time is part-time, calendar dates stretch proportionally. The estimates will be re-baselined after Phase 1, once real velocity is known.
>
> **Why OKRs moved forward.** The year-end bonus is driven by KPI and OKR results (SRS D13). For the 2027 bonus to be computed in-system, 2027 goals and KPI actuals must be captured from early 2027 — so goal and KPI tracking ships as Phase 3.5, and the review cycle and bonus scheme follow in Phase 8, before year-end 2027.

---

## 2. Architecture plan

### 2.1 Application shape — modular monolith

A single Next.js application and a single PostgreSQL database, strictly modular inside. There are no microservices and no monorepo tooling until a second deployable actually exists.

```
src/
  app/                      # Next.js App Router — routes only, thin
    (auth)/                 # sign-in, not-provisioned
    (app)/                  # authenticated shell
      home/  people/  attendance/  leave/  payroll/  work/  ops/
      kb/  recruit/  performance/  assets/  requests/  admin/
    (public)/careers/       # the only public surface
    api/                    # webhooks, device push, cron endpoints
  modules/                  # ← all business logic lives here
    platform/
      auth/  org/  rbac/  approvals/  tasks-engine/  notifications/
      files/  audit/  search/  statutory/  import-export/  jobs/
    core-hr/  attendance/  leave/  payroll/  work/  ops/  kb/
    recruit/  performance/  assets/  requests/  comms/  ai/
      ├─ schema.ts          # Drizzle tables owned by this module
      ├─ service.ts         # use-cases; the only entry point for other modules
      ├─ policy.ts          # authorization rules for this module
      ├─ actions.ts         # server actions (validate → authorize → service)
      ├─ events.ts          # domain events emitted / handled
      ├─ engine/            # pure calculation code (payroll, timesheet, accrual)
      └─ ui/                # module components
  lib/                      # db client, env, i18n, money, dates, utils
  components/ui/            # shadcn design-system components
docs/  drizzle/ (migrations)  tests/  scripts/
```

Rules (enforced by lint boundaries):

- `app/` contains no business logic. It calls module actions and services.
- A module imports another module **only through its `service.ts`**, or reacts to its **domain events** (e.g. `employee.hired` → ops tracker creates the "register insurance" obligation; `leave.approved` → attendance recomputes the day).
- Every server action follows one pipeline: **parse (zod) → authenticate → authorize (policy) → service → audit → revalidate**. A shared `createAction()` wrapper makes skipping a step impossible.
- Calculation engines (`payroll/engine`, `attendance/engine`, `leave/engine`) are **pure functions with no I/O**. They take plain data and statutory parameters and return results with an explanation trace. This makes them testable against the accountant's spreadsheets.

### 2.2 Key technical decisions

| # | Decision | Rationale |
|---|---|---|
| ADR-01 | Next.js 16 App Router, server components and server actions; Node.js runtime (no edge runtime) | Simple data access and full Node APIs for PDF/Excel generation |
| ADR-02 | PostgreSQL + Drizzle ORM; SQL migrations committed and reviewed | Integrity constraints, effective-dated queries, reporting SQL |
| ADR-03 | Better Auth with Google provider; database sessions | Instant revocation on offboarding; custom two-workspace `hd` check (FR-PLT-02..05) |
| ADR-04 | Authorization in an application policy layer (role × scope × sensitivity tier); DB access only through module services | "Manager-of" and effective-dated scopes are impractical as row-level policies alone; one place to test |
| ADR-05 | Application-level envelope encryption for `restricted` / `compensation` columns | Defence in depth beyond disk encryption (DR-05) |
| ADR-06 | Money as integer VND in a `Money` helper type; no floats | Payroll correctness (DR-01) |
| ADR-07 | Effective-dated tables use `valid_from` / `valid_to` with exclusion constraints | History without ambiguity (DR-02) |
| ADR-08 | Domain events via a transactional outbox table + background dispatcher | Reliable cross-module reactions without coupling |
| ADR-09 | Background work: cron triggers + durable workflow steps for payroll runs, imports, schedulers and notification fan-out | Long jobs survive restarts and show progress |
| ADR-10 | One task engine; work management and the ops tracker are "task kinds" with their own views | One "My work" inbox, less code |
| ADR-11 | Feature flags by entity/department/user | Pilot rollouts (NFR-OPS-05) |
| ADR-12 | next-intl; Vietnamese as source language | Vietnamese-first UX |
| ADR-13 | Hosting Vercel (Singapore) + Supabase Postgres/Storage (Singapore), domain `suzu.one` — decided (SRS D10, D11); everything containerisable | Speed now, portability later |

### 2.3 Environments & pipeline

| Environment | Purpose | Data |
|---|---|---|
| Local | Development: self-hosted Supabase stack in Docker via the Supabase CLI, seeded fake company | Synthetic |
| Preview | One per pull request, auto-deployed; database branch | Synthetic |
| Staging | UAT with HR and the accountant; payroll parallel-run rehearsals | Anonymised copy |
| Production | Live | Real |

CI on every pull request: typecheck → lint (incl. module boundaries) → unit tests → integration tests (real Postgres) → permission-matrix tests → build → preview deploy → Playwright smoke tests on the preview. The main branch deploys to staging automatically. Production deploys by manual promotion.

---

## 3. Phase details

Each phase lists its scope (SRS IDs), key deliverables, and its **exit criteria** — the conditions under which the phase counts as done.

> ### MVP status — read this first
>
> **Phases 0–9 are built.** Phases 1–9 sit on local branch `mvp` (not pushed, not merged, not deployed); only the first part of Phase 0 is live on `main`. `pnpm check` (2,498 tests) and `pnpm build` pass; migrations 0001–0071, all additive.
>
> **[`docs/MVP_STATUS.md`](./MVP_STATUS.md) consolidates all ten phases into one page**: what is built, what is deferred, what has never been seen in a browser, and — the part that matters — a **single ordered "Needs the owner" checklist** gathering every external key, account, DNS entry, production seed, legal and accounting confirmation and real-data task the nine phases recorded. Start there, not here, when the question is "what has to happen before this goes live".
>
> The status note under each phase below remains the detailed record for that phase.

### Phase 0 — Foundation & platform (3 weeks)

> **Status, 2026-09-19.** The first part is live on `main`; the remainder below is built on branch `phase-0-remainder` (stacked on `phase-1-core-hr`; not merged, not deployed).
> **Done (on `main`):** Next.js 16 scaffold and tooling (`pnpm check`); schema + migrations for auth, entities/branches/shared departments/teams, person, role assignments, audit log (append-only trigger); Google sign-in with the two-workspace rules (`sign-in-policy.ts`, tested); per-request access re-check with session revocation; RBAC policy (role × scope × tier, tested); `createAction()` pipeline with audit; vi/en i18n; app shell, home, entities admin (list + create); seed data; security headers; local Supabase stack in Docker; row-level security on every table with the Supabase API roles revoked; Vercel project linked, production env set, migrations run on production deploys; `suzu.one` attached to the project.
> **Done (on the branch):**
> - *Engineering:* CI workflow (dependency audit → typecheck → lint → tests → build); lint rules for module boundaries and for `env()`-only configuration; the permission matrix generated from the role catalogue into `docs/permission-matrix.md` and checked by a test.
> - *Scheduled jobs:* `job_run` log with overlap protection and stale-run takeover; `/api/cron/*` behind `CRON_SECRET`; two daily schedules in `vercel.json`; failures notify the Owners; Admin → Scheduled jobs. First job: the daily people roll-over (future-dated assignments reach `person`, pre-boarding people become active) — this closes Phase 1 gap 1.
> - *Admin screens:* entity edit and branches; departments and teams (with parent-loop and still-in-use guards); role grants (grant, revoke, last-owner guard, person notified); audit-log viewer scoped by `entityReach()`.
> - *Notifications:* in-app centre with unread badge; email through an outbox (Resend; retries, gives up after five attempts); per-category preferences with mandatory security notices; daily digest. In use for role changes, work-email changes (the old address is told too — closes Phase 1 gap 7), failed jobs and rule proposals.
> - *Files:* private Supabase Storage bucket, direct browser upload through signed URLs, type allowlist + size limit + magic-byte check on what actually arrived, one-minute download links, read-audit for restricted/compensation files, soft delete, abandoned-upload cleanup. Verified against the local Supabase stack (`tests/storage.integration.test.ts`).
> - *Encryption:* envelope encryption for restricted/compensation fields (per-value data key, AES-256-GCM, bound to its row and column, re-wrap for key rotation) and a blind index for look-ups; `docs/KEY_ROTATION.md`.
> - *Statutory parameter store:* typed catalogue (money in VND, rates in basis points), effective-dated versions with a database exclusion constraint, propose → owner decides (FR-PLT-39, new permission `rules:propose`), seeded from SRS Appendix A as **unverified** until confirmed.
> - *Feature flags:* rollout per everyone / entity / department / person; the People module is the first flagged module (HR and admins always see it).
> - *Import framework:* `.xlsx`/`.csv` → typed rows + every problem in one pass → stored preview → all-or-nothing commit by the same person within a day; generic wizard and CSV template; first import: departments.
> - *Error tracking:* `instrumentation.ts` logs every server error as one structured line (no query strings) and forwards it to Sentry (`SENTRY_DSN`, falling back to `NEXT_PUBLIC_SENTRY_DSN`, which is the one the Vercel integration sets). Failed jobs, failed public actions and browser errors are reported the same way, still without an SDK (`src/lib/observability`); localized error and not-found pages. Sentry forwarding verified in production on 2026-09-23.
> **Verified locally:** signed-out redirects, forged cookie rejected, offboarded person locked out with a still-valid session; the real Google sign-in round trip (owner signed in with `khanh@suzu.vn`, 2026-09-19); every new action over HTTP as owner, HR admin, entity HR staff, finance, department head and employee (allowed and refused paths, audit entries); cron endpoints with and without the secret; file storage against the local stack; production build and a smoke test of it.
> **Not yet verified:** any screen in a real browser or on a phone (forms, the import wizard's file picker, the browser's direct upload to storage, error pages); email delivery through Resend; the cron triggers on Vercel.
> **Needs the owner before it can go live:** DNS for `suzu.one`; `CRON_SECRET`, `DATA_ENCRYPTION_KEYS` (+ offline copy), `DATA_BLIND_INDEX_KEY` in Vercel; a Resend account with the `suzu.one` sender domain verified; confirm the Supabase integration exposes `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`; branch protection on `main` requiring the CI check; `pnpm db:seed` against production for the statutory parameters.
> **Still open in Phase 0:** virus scanning of uploads (no scanner chosen — files are marked `not_scanned`); a strict Content-Security-Policy (NFR-SEC-01); rate limiting on auth (NFR-SEC-03); step-up re-authentication for compensation screens (FR-PLT-06, needed by Phase 5); secret scanning in CI; preview deployments with a database branch and Playwright smoke tests; the working calendar and public holidays (moved to Phase 2 with attendance); Google Chat notifications (S); structured editors for statutory parameters instead of JSON (before Phase 5); list export (FR-PLT-37, with Phase 1).

**Scope:** FR-PLT-01..06, 10..14, 30, 32, 33, 35, 38 (store only) · FR-ACL-01..04 · NFR-SEC · NFR-OPS-04/05

| Week | Deliverables |
|---|---|
| 1 | Repo, tooling, CI pipeline, environments, Vercel + database provisioning, design-system shell (sidebar, command palette, light/dark, vi/en), Google Cloud project + OAuth client (External) on `suzu.one`, sign-in with `hd` allowlist, not-provisioned page, bootstrap owner |
| 2 | Org model (group/entity/branch/**shared departments**/team, effective-dated) with **editable seed entities and departments** (SRS D8), person + user linkage, RBAC (roles, scopes, tiers, incl. the restricted Collaborator role), `createAction()` pipeline, audit log, admin screens for entities, org and roles |
| 3 | Notifications (in-app + email), private file storage with signed URLs, statutory parameter store, import framework (Excel → validate → preview → commit), feature flags, error tracking, seed script for a realistic fake company |

**Exit criteria**
- A `suzu.vn` and a `suzu.group` account can both sign in; a personal Gmail account cannot; a deactivated user is signed out within one request.
- The permission-matrix test suite exists and runs in CI.
- Every mutation in the app so far appears in the audit log.

### Phase 1 — Core HR (4 weeks) → **M1**

> **Status, 2026-09-19 — all four weeks built on local branch `mvp` (not pushed, not merged, not deployed); `pnpm check` (225 tests) and `pnpm build` pass. Exit criterion "a line manager cannot see restricted or compensation fields" is proven by the PGlite service tests (`records.test.ts`, `change-requests.test.ts`, `lifecycle.test.ts`) and over HTTP; the other two exit criteria (real import verified by HR, spreadsheet retired) need the owner and HR. Week-by-week notes follow; week 1 first (built on `phase-1-core-hr`).**
> **Done:** `core-hr` module — employment periods (one per person at a time, rehire-safe), effective-dated primary assignments (department, team, branch, position, job level, line and dotted-line manager, workforce type) with database exclusion constraints, shared position catalogue, per-entity employee-code numbering, personal-tier profile table, saved list views; people list with search, filters, given-name sorting and paging; add-person, edit and change-assignment forms; person page shaped by sensitivity tier; `tierReach()` (the list form of `readableTier`, matrix-tested against it); work email made optional (people without app access); local-only demo company (`pnpm db:seed:demo`).
> **Verified locally** as owner, entity HR, department head and plain employee: list and person-page tiers, hire, future-dated and corrected assignments, every rejection path, audit entries.
> **Tests:** pure engines, policy (incl. a matrix test that `tierReach` equals `canReadTier`), migrations, and service tests on PGlite that check the list SQL against the policy for every kind of viewer.
> **Known gaps:** (1) ~~future-dated assignments and start dates reach `person` only on the next save~~ — closed by the daily roll-over job on `phase-0-remainder`. (2) FR-CHR-01 is partly covered: education, certificates and photo are not modelled yet (~~emergency contacts, restricted fields~~ — closed in week 2). (3) ~~No duplicate-person warning on add, and no rehire screen~~ — closed in week 4. (4) No screen for the employee-code scheme (defaults to `<ENTITY>-0001`). (5) ~~The list has no export yet (FR-PLT-37)~~ — closed in week 4 (CSV). (6) ~~Forms show one message per failure, not per-field errors~~ — per-field errors in week 4 (hire and edit forms; other forms only need the `FieldErrors` wrapper); still not clicked through in a browser or checked on a phone. (7) ~~no notification when a work email is re-pointed~~ — closed on `phase-0-remainder`: the old address and the person are both told. (8) Secondary assignments have a schema but no screens. (9) Page `<title>`s are hard-coded English, as in Phase 0.
> **Week 2, 2026-09-19 — built on local branch `mvp` (not pushed, not deployed).**
> **Done:** *Restricted fields* (`person_sensitive`: citizen ID with issue date/place, passport, tax code, social insurance number, registered hospital, bank accounts) — envelope-encrypted per row and column, blind index on the citizen ID; the person page shows only which fields are filled, values arrive on an audited "Show" (`person.sensitive.read`), and the audit log records the names of changed fields, never values. *Contracts* (seven types, appendices, early termination, soft delete, encrypted salary terms, signed copy as a file) with a pure rules engine fed from the statutory store: fixed term ≤ 36 months, one fixed-term renewal then indefinite, probation length by job category, one probation per employment, no overlapping labour contracts. *Dependents* register (restricted tier; encrypted ID/tax code; deduction months; supporting papers). *Document vault* on the files module (signed-URL upload from the browser, tier fixed by category: ID scans and health checks restricted, signed contracts and decisions compensation, the rest personal; expiry dates). *Emergency contacts* (personal tier). *Daily `hr-alerts` job*: contract expiry 45/30/15 days, probation end 10/3 days, document expiry 30 days — thresholds are the new owner-approved parameter `hr.alert_thresholds`; idempotent through `hr_alert_sent`; new notification category "HR deadlines" (in-app + daily digest by default). *Key rotation*: the promised re-wrap job (`/api/cron/field-keys-rewrap`). Demo seed extended (contracts about to expire, restricted details, a dependent).
> **Decisions:** contract type, number and dates are *personal* tier (the line manager plans around them and is alerted); salary terms and signed copies are *compensation* tier. Writing a record takes `person:manage` **and** the right to read that tier — so entity HR staff (restricted) keep restricted records but cannot record salary terms or attach signed contracts; HR admin can (revisit if entity HR should file signed copies). Employees read all of their own records but change nothing directly — that is week 3's change requests. Alerts go to roles that name `person:manage` (HR) plus the line manager — not to owners, whose `*` would bury them; document-expiry alerts go to HR and the person, not the manager. Probation alerts come from probation *contracts*, not from the workforce type. No new RBAC permissions.
> **Verified locally** over HTTP as HR admin, entity HR staff, department head / line manager and the employee: page sections per tier; reveal allowed/refused with audit entries and no plaintext in the audit log; every contract rule rejection; out-of-scope and tier refusals; vault and signed-copy uploads end to end against local storage (wrong type, re-labelling a category and unconfirmed uploads refused; one-minute download; `file.read` audited); the alerts job through `/api/cron/hr-alerts` (first run sends, second run sends nothing); the re-wrap job. **Tests:** golden tests for the contract rules and the alert countdown; policy tests; PGlite service tests for the exit criterion — a line manager and a department head get no restricted fields, no dependents, no restricted/compensation documents and no salary terms, HR staff stop at restricted, encrypted columns hold no plaintext.
> **Not yet verified:** the forms and the browser's direct upload in a real browser or on a phone; email delivery of the alerts.
> **Needs the owner:** `pnpm db:seed` on production adds the `hr.alert_thresholds` parameter (without it the alerts job fails and says so); `DATA_ENCRYPTION_KEYS` and `DATA_BLIND_INDEX_KEY` must be set before anyone saves restricted details.
> **Deferred:** education and certificates as structured records (the vault holds the scans), photo, editing a contract in place (delete and re-enter), bank-account change verification (with change requests, week 3), denied audit entries do not name the target person (pipeline-wide), contract import (FR-PLT-36), document generation (FR-CHR-06, Phase 6).
> **Week 3, 2026-09-19 — built on local branch `mvp` (not pushed, not deployed).**
> **Done:** *Approval engine* (`src/modules/platform/approvals/`, first use): `approval_request` / `approval_step` / `approval_assignee` / `approval_event`; a pure, golden-tested flow state machine that already covers sequential steps, "any" and "all" steps, conditions on request data, return-for-changes + resubmit, withdraw and delegation; approver rules (line manager, N-th level manager, department head, role, permission holder, named person) turned into people at submission and snapshotted on the request; nobody approves their own request, and a step nobody can answer falls back to the owners; inbox `/approvals` ("waiting for me" with a badge in the navigation, "my requests"); new notification category "Approvals" (`approvals.requested`, `approvals.decided`). Request types are definition objects owned by their module, which also owns the decide action — the engine decision and the business effect run in one transaction. *ESS* `/me`: the signed-in person's whole record, read-only (restricted values through the audited reveal), not behind the People flag. *Change requests* (`profile_change`): phone, personal email, addresses, marital status, citizen ID (+ issue date/place), tax code, social insurance number and the pay bank account; personal values are stored as a before → after diff, restricted values only in the request's encrypted payload (names in the plain payload and the audit log); one open request per person; HR with `person:manage` over the person decides — approve applies the change in the same transaction, reject and return need a comment, the requester withdraws or corrects and resubmits; **a bank-account change cannot be approved without the approver confirming a second-channel check**, recorded on the decision; pending requests show on the person page for HR. *Org chart* `/people/org-chart`: built from line-manager links of active people, per entity or group-wide, collapsible, directory-tier fields only, dotted lines as a note. *Employee bulk import* `/people/import`: 28 columns (Vietnamese headers first), every problem in one pass (duplicates in the file and on the books — work email, employee code, citizen ID through the blind index; unknown entity/department/team; email domain; the importer's reach **per row**; manager not found / self / loop), all-or-nothing commit through the same code path as the hire form, managers from the same file linked in a second pass. The import framework gained `sensitive` columns: those cells are encrypted while a batch waits for its commit and masked in the preview. Demo seed: two pending change requests (personal, bank account).
> **Decisions:** the owning module owns the decide action (platform code cannot import feature modules) — Phase 2's bulk approve needs a type → definition registry at a composition root, noted in `engine/flow.ts`; withdrawing is generic (`approval.withdraw`). Approvers by permission are the roles that *name* it (HR), not the owners' `*`. Deciding a change request takes being asked by the flow **and** still holding `person:manage` over the person **and**, when the request carries restricted values, reading the restricted tier; a line manager can neither open nor decide one. An approved bank account becomes the first (pay) account, the others are kept. Restricted fields can be replaced through a request, not cleared. Name, date of birth, gender and work email stay HR-only corrections. The import adds new people only (no updates), does not take branches, dotted-line managers or passports, and stores the staged restricted cells encrypted rather than leaving them out, so the commit needs no second upload. The directory stays the tier-shaped people list; **phone stays personal-tier**, so neither the list nor the org chart shows it until the owner decides otherwise. No new RBAC permissions.
> **Verified locally** over HTTP as the employee (submits, is refused a second request, withdraws, resubmits after a return, cannot decide), entity HR staff (reveals and decides inside the entity; 404/forbidden for another entity's employee; refused a bank change without the confirmation), HR admin (decides with the confirmation; commits only own import batch) and the line manager / department head (request pages 404, decide / reveal / withdraw / import refused, no change-request section on the person page): `/me`, `/approvals`, request page, org chart (group and one entity), import stage with problems, stage + commit, second commit refused; audit entries for every step incl. `.denied`; notifications and outbox wording; no restricted plaintext in `approval_request`, `import_batch` or the audit log. **Tests (186):** golden tests for the flow engine and the org tree; policy tests; PGlite tests for the approval service through change requests (approver resolution, owner fallback, approve applies, reject, return + resubmit, withdraw, own request, non-assignee, bank verification, approver below the restricted tier, who may open and reveal) and for the import (one-pass problems, reach per row, commit, encrypted staging).
> **Not yet verified:** the forms in a real browser or on a phone; email delivery; an `.xlsx` employee file (CSV was used; the parser path is shared with the department import); the org chart with a few hundred people.
> **Needs the owner:** nothing new — `DATA_ENCRYPTION_KEYS` must be set before change requests with restricted fields or imports with restricted columns are used. HR should format phone columns as text in Excel (leading zero).
> **Deferred:** approval flows configurable per entity in the database, parallel steps, delegation and bulk-approve screens, SLA reminders (Phase 2: FR-PLT-20..24 — the state machine and tables are ready); comments on a request without deciding; change requests for dependents, emergency contacts and documents (HR enters them); showing restricted "before" values inside the request (the approver uses the audited reveal on the person page); import of updates to existing people, contracts and leave balances; a card-style directory and an opt-in work phone.
> **Week 4, 2026-09-19 — built on local branch `mvp` (not pushed, not deployed).**
> **Done:** *Task engine* (`src/modules/platform/tasks-engine/`, first use): one `task` table for every kind of work (kind, generic status category, assignee, due date, priority, entity, parent task, context type + id, subject person, soft delete) with `task_template` / `task_template_item`; a pure, golden-tested checklist engine (most specific active template wins: position > department > entity > group; items + anchor date + assignee rules → dated, ordered tasks; progress summary); assignee rules subject / line manager / named person / permission holder (the entity's own HR before group HR, never the owners' `*`, never the subject); policy by kind (`KIND_MANAGE_PERMISSION`: checklists → `person:manage`; the assignee moves their own task, only a manager cancels or reassigns); `/tasks` ("My tasks", overdue first, badge in the navigation), Admin → Checklist templates; notification category "Tasks" (one notice per person per checklist; in-app + digest); starter onboarding and offboarding templates seeded by `pnpm db:seed`. *Lifecycle events* (`lifecycle_event`, 13 types, pending / applied / cancelled): hire and rehire are written by the one hire path (form and import) and start the onboarding checklist (the import only for people still to start); a change of assignment is now a transfer, a promotion or a plain correction (no event), with a from → to snapshot in words; hand-recorded events (probation result, renewal, reward, discipline, long leave, salary change without amounts); timeline with each event's checklist on the person page and on `/me`. ***Termination ends access***: the employment ends on the last day (inclusive), open assignments and contracts stop that day, assignments that would start later are dropped, every role grant ends that day (the last owner is refused), the offboarding checklist starts — and once the last day has passed (at once for a past date, otherwise through the daily roll-over job) the person becomes `offboarded` and their sessions are deleted; a termination that has not taken effect can be called off, which reopens everything it closed. *Resignation requests* (second request type on the approval engine): employee submits on `/me` → line manager (owner fallback) → an approval leaves a pending resignation event and notifies HR, who carry it out as a termination from the person page in one step. *Rehire* on the same person record (new employment period and employee code, onboarding checklist) and a *likely-duplicate warning* on hire (name + birth date, personal email, phone; the hire is refused until HR confirms). *Headcount report* `/reports/headcount` (`report:read`): headcount on a date by entity / department / workforce type / gender / age band / seniority band, joiners, leavers, turnover over the average headcount (integer basis points), contracts ending within 90 days, probation pipeline; scoped by the new `permissionReach()` (matrix-tested against `can`) so an entity director or department head sees their slice only. *List export* (FR-PLT-37): CSV with BOM and a formula-injection guard; the people list and the headcount report export through `createAction` (audited with filters and row count) by calling the screen's own service function with the viewer's principal. *Per-field form errors*: actions return zod issue codes per field, `<Field>` shows them localized.
> **Decisions:** request-type-like composition again — the task engine knows nothing of HR; core-hr hangs checklists on `lifecycle_event` contexts. An approved resignation ends nothing by itself: HR confirms the last day and applies it (notice periods and handover are HR's call). A rehire gets a new employee code (codes are unique per entity and the old one stays with the old period); seniority restarts unless HR enters a seniority date. Discipline notes are restricted tier (the line manager sees that the event happened, not the note); the audit log keeps `[restricted]` instead of the text. Future-dated transfers are stored as applied and shown as "not in effect yet" by date. Reports are aggregates plus two lists of personal-tier facts the reader already sees in the people list; HR staff have no `report:read` in the role catalogue, so they do not see reports (owner's call). `ActionError` can carry `details` for the form (used for the duplicate candidates). No new RBAC permissions; the permission matrix is unchanged.
> **Verified locally** over HTTP as HR admin, entity HR staff, department head / line manager, employee and a new hire: hire refused with duplicate candidates then confirmed, per-field errors, onboarding tasks with the right people and dates, own task completed, other people's tasks / cancel / reassign refused, HR reassigns and completes, promotion event, **a terminated person's live session is redirected to sign-in on the next request and their session rows are gone**, grants and assignments ended, second termination refused, rehire (one person, two employment periods), resignation submit → second one refused → HR and colleague cannot decide → manager approves → HR notified → termination from the resignation (still signed in until the last day) → called off (employment, assignment reopened, checklist cancelled), templates (entity HR may not write shared ones), headcount for HR admin (group) and department head (department only), 404 for employee and entity HR staff, exports with audit rows and `.denied` rows, people export as a line manager (personal-tier columns only for his reports, former employees absent). **Tests (225):** golden tests for the checklist engine, headcount engine and CSV; task policy; `permissionReach` vs `can` matrix; PGlite tests for hire → event + tasks, termination now / later / called off / last owner, rehire, duplicates, resignation path, timeline tiers, scoped headcount, export contents. `pnpm check` and `pnpm build` pass.
> **Not yet verified:** any of this in a real browser or on a phone (the forms, the CSV download, the checklist buttons); the digest email wording for task notices.
> **Needs the owner:** `pnpm db:seed` on production to get the starter checklists (then HR edits them); decide whether entity HR staff should read headcount reports (`report:read` is not in `hr_staff` today); run the real employee import and have HR verify it per entity (Phase 1 exit criterion — cannot be done without the real spreadsheet).
> **Deferred:** Excel/PDF export formats; exports on the admin lists (audit log, roles); task due-soon / overdue reminders; sub-tasks, comments, attachments and boards (Phase 3 on the same table); pre-boarding self-service form (FR-CHR-10 second half — new hires can already sign in before day 1 and use change requests); generated documents for events (FR-CHR-06, Phase 6); approval on transfers and promotions (events are HR-recorded; only resignation goes through the approval engine); calling off a termination does not put its resignation event back to pending; a returned resignation is withdrawn and re-filed rather than edited; in a bulk import, a future starter whose manager sits lower in the file gets the manager's checklist step unassigned; joiners / leavers / turnover reports per month series, attendance and leave reports (Phase 2); employee-code scheme screen, secondary-assignment screens, localized page titles (known gaps 4, 8, 9).

**Scope:** FR-CHR-01..05, 07..13 (M items) · FR-PLT-36/37 · FR-RPT-02 (headcount subset)

| Week | Deliverables |
|---|---|
| 1 | Person and employment records, workforce types, employee codes, effective-dated assignments, people list with filters and saved views |
| 2 | Contracts (types, rules, expiry and probation alerts), dependents, document vault, sensitive-field encryption and read-auditing |
| 3 | ESS profile + change requests (first use of the approval engine: single-step flows), directory, org chart, bulk import of all existing employees |
| 4 | Lifecycle events (hire, transfer, promotion, termination), onboarding/offboarding checklists (first use of the task engine: checklist tasks), headcount reports, UAT and data migration |

**Exit criteria**
- 100% of current employees imported and verified by HR for every entity.
- HR confirms the employee spreadsheet is no longer the source of truth.
- A line manager cannot see restricted or compensation fields (verified by test and by UAT).

Deferred to later phases: document generation from templates (FR-CHR-06, Phase 6), skills inventory (FR-CHR-14, Phase 3), headcount planning (Phase 7).

### Phase 2 — Attendance & leave (5 weeks) → **M2**

> **Status, 2026-09-20 — all five weeks built on local branch `mvp` (not pushed, not merged, not deployed); `pnpm check` (479 tests) and `pnpm build` pass; migrations 0018–0027. No new RBAC permissions (`attendance:manage`, `leave:manage`, `org:manage` only); the permission matrix is unchanged. None of the three exit criteria can be met without the owner: they need a pilot department running a real month, real opening balances and production traffic.**
> **Done — approval engine (FR-PLT-20..22, `src/modules/platform/approvals/`):** flows configurable per request type and entity in the database (`approval_flow`; entity flow > group flow > the type's code default; validated, at least one unconditional step; Admin → Approval flows, `org:manage`); parallel steps in the pure state machine (golden-tested); conditions on request data; standing delegation (`approval_delegation`, applied when approvers are resolved, `/approvals/delegation`), handing one request to someone else, commenting without deciding; bulk approve from the inbox — each request goes through its owning module's own decide action (own authorization, transaction, audit row) via the type → definition registry at the composition root `src/app/(app)/approvals/registry.ts` (**every new request type must be registered there**); a later step whose only approver already answered an earlier step is skipped. Request types now: `profile_change` (bulk only without restricted fields or a bank account), `resignation` (never bulk), `leave`, `attendance_correction`, `remote_work`, `overtime`, `holiday_work`.
> **Done — attendance (`src/modules/attendance/`):** *Calendar and schedules* (FR-ATT-01, 02, 17): `calendar_day` rows (public holidays 2026–27 seeded as **unconfirmed** data, compensatory and company days off, working overrides; an entity's row beats the group's), shifts (split, overnight), schedules (fixed / flexible / shift, alternate Saturdays, **Saturday = untracked work-from-home day by default**), effective-dated assignments per entity / department / person with exclusion constraints, shift roster, pure day-plan engine. *Check-in* (FR-ATT-03, 04): `work_location` (geofence and/or office public-IP allowlist, mode flag | block), `punch` (server clock only, position + accuracy, IP, device info, flags, review status), pure geofence engine, `/attendance/check-in` (mobile-first; double tap = one punch; overnight check-in stays open; leave = warning), flagged-punch review by the line manager or HR, "who's in today". *Installable PWA*: manifest, locally generated placeholder icons, service worker that caches static assets only (never pages, RSC payloads or action responses), offline page, push handlers, folding mobile navigation. *Web push* in `platform/notifications`: subscriptions, push outbox, per-category push preference, local driver (records `simulated`) when VAPID keys are absent, real driver on node:crypto whose encryption reproduces the RFC 8291 example byte for byte. *Device import* (FR-ATT-06) on the import framework (extended, backward-compatible: parameters beside the file, custom readers, non-blocking warnings): clocks, mapping profiles (CSV, Excel, ZKTeco `attlog.dat`), device-ID → person map with bulk mapping by employee code, idempotent re-import (unique device + ID + time), unmapped IDs wait and become punches when mapped, CSV report. *Policy*: effective-dated `attendance_policy` per entity (merge rule, grace, rounding, minimum OT, OT needs approval, duplicate window, day boundary, monthly correction cap); new statutory parameter `work.night_window` (unverified). *Engines* (pure, golden-tested): merge (FR-ATT-08: first-in/last-out across sources by default, prefer device, prefer app), daily timesheet (FR-ATT-09: late, early, missing punch, absence, half/full leave, holiday, untracked-Saturday credit, WFH, off-site, OT weekday / rest day / holiday with night minutes kept separately, overnight and split shifts, flexible hours, explanation trace), month summary. *Stored timesheet*: `timesheet_day` with an inputs hash; recompute on every input change (punch, review, import, mapping, leave, calendar, schedule, roster, policy, request) plus a nightly job; locked rows are never rewritten. *Requests* (FR-ATT-10, 11, 12, 18): correction (writes `request` punches; monthly cap from the policy; HR on behalf is not capped), remote work (WFH / off-site / business trip; an off-site place with coordinates becomes a valid check-in location), overtime and holiday / rest-day work (pay or time off in lieu; hours from punches or **confirmed by the manager** where punches cannot exist; warnings — never refusals — against `overtime.caps`). *Monthly timesheet* (FR-ATT-14): employee confirms → line manager approves (HR may approve without the confirmation) → HR locks per entity + month; the lock is refused with a list of blockers unless HR overrides with a reason (stored with the exceptions and audited), freezes days and totals, and posts time off in lieu to the leave ledger; no unlock — `timesheet_adjustment` (HR, audited, retro item for the next payroll). *Views* (FR-ATT-15): my month with per-day explanation and one-tap fix, team grid, manager and HR timesheet progress, HR anomaly console with links to the fix, nudge and CSV export; notification category "Attendance".
> **Done — leave (`src/modules/leave/`, FR-LVE-01..05, 07, 08):** leave types with every FR-LVE-01 attribute (11 starter types seeded), effective-dated policies, pure golden-tested entitlement / accrual engine (statutory base + seniority step from the parameter `leave.annual`, company extras, pro-rata, monthly accrual or yearly grant, probation rule, carry-over cap + expiry, payout on termination), append-only ledger in hundredths of a day with idempotent source keys, HR adjustments with a reason, opening-balance import, nightly accrual / carry-over / lapse / payout job; request flow on the approval engine (line manager, + department head above 3 days — FR-PLT-21) with balance, working-day count on the person's own calendar, notice / maximum / attachment / eligibility checks, colleagues away and minimum-staffing warnings, cancel with refund, amend, HR on behalf; team calendar; long-term leave records a core-hr lifecycle event. **Leave approval and cancellation are refused inside a locked timesheet month** (`leave_period_locked`) — HR records a timesheet adjustment instead.
> **Payroll-facing API (Phase 5):** `@/modules/attendance/service` → `getLockedTimesheets(entityId, month)` (null until locked; per person: standard days, paid / unpaid days, worked, credited, leave, holiday, absence, late / early, night, OT per category with day / night split, time-off-in-lieu vs payable minutes, lock metadata), `listAdjustmentsForPayroll(entityId, payrollMonth)`, `markAdjustmentsTaken(tx, ids, payrollMonth)`, `isPeriodLocked`. `@/modules/leave/service` → `getLeaveUsage(scope, from, to)` (split by type and payroll treatment), `getBalances`, `getLedger`, `listPayouts`. Payroll takes totals from the locked timesheet and only the split by leave type from leave; OT multipliers stay in the statutory store — nothing is multiplied in attendance.
> **Decisions:** the browser cannot read a Wi-Fi name, so the "Wi-Fi rule" is the office public-IP allowlist; no offline check-in queue (punch time is the server's clock); out-of-policy check-ins are accepted and flagged, blocked only when every location of the entity says block; positions are personal tier (the person, the line manager, HR in scope — never colleagues or department heads), timesheets are personal tier, colleagues see only *that* someone is away or in; untracked Saturdays cost no annual / personal leave but do count for sick, maternity, unpaid and sabbatical (per type); leave year = calendar year, pending requests hold balance; long-term leave leaves the person `active` (suspending would lock the account); a missing punch makes the day's worked time unknown, not guessed; arriving early is never overtime; work on a holiday or rest day counts only with an approved request; WFH and trips are credited without punches; every attendance request goes to the line manager only by default (entities add an HR step on the flows screen); time off in lieu is posted at the lock, one-for-one; adjustments need no second approver. **Client IP:** the first `X-Forwarded-For` entry was client-controlled — audit log and punches now use `x-real-ip`, else the last forwarded entry; without a trusted proxy the headers can still be spoofed, so GPS + review remain the main control.
> **Verified locally** over HTTP with forged sessions as owner, group HR admin, entity HR staff, department head / line manager, employees, a probationer, a colleague and another entity's department head — every new action's allowed and refused paths with audit rows incl. `.denied`: flows, delegation, comments, bulk approve; calendar / schedule / assignment / roster settings; leave submit / refuse / amend / cancel / two-step approval / adjust / import / accrual cron; check-in inside, outside, without position, from a foreign IP, block mode, review; manifest, service worker and headers with curl; simulated push from a real leave request; device import → preview → commit → re-import adds nothing → mapping resolves waiting lines; policy versioning; nightly recompute with and without the secret; **the whole lock flow on the demo data** — correction → approval → day recomputes → employee confirms → manager approves (single and bulk) → HR sees blockers → locks one entity in one call and another with an override → a later punch and the nightly job leave the locked month untouched → adjustment appears for payroll. Check-in on the production build against the local database: median 34 ms, p95 43 ms over 30 calls. **Demo data** (`pnpm db:seed && pnpm db:seed:demo`, then `pnpm dev` + `pnpm db:recompute`): opening balances and accruals for 2026, 12 leave requests, 1,104 punches from 1 Aug to 18 Sep 2026 from two clocks and the app with scripted anomalies, 13 attendance requests, August timesheets — **SZM's August locks in one click, SZC shows four blockers, SZG is half-way**, September is open; clock export fixtures in `scripts/fixtures/`.
> **Not yet verified:** anything in a real browser or on a phone — the location prompt, the install prompt, service-worker registration and caching, the push subscription round trip and a delivered push, the `.dat` file picker, evidence upload, CSV downloads, the grids on a small screen; an `.xlsx` clock export; real exports from the company's clocks (models unknown, SRS Q5); a check-in at a declared off-site place over HTTP (unit-tested); check-in p95 under real morning load.
> **Needs the owner:** `pnpm db:seed` on production (holidays, default schedule, leave types incl. `COMP`, attendance policy, clock profiles, `work.night_window`) — without a compensatory leave type a lock that carries time off in lieu fails with a clear message; HR to confirm the 2026–27 holiday rows and the compensatory days the government announces; the accountant to verify `leave.annual`, `overtime.caps`, `overtime.multipliers`, `work.night_window`; `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` in Vercel (`pnpm push:keys`) and one real phone test (iOS pushes only to the installed app); real office coordinates and public IPs per entity; designed app icons; one curl against production to confirm a client-sent `X-Real-IP` is overwritten; clock models and export formats (then adjust the mapping profiles); per entity: grace / rounding / OT rules, the monthly correction cap (default 3), whether corrections or holiday work get an HR step; real opening balances through the import; the pilot month and HR's 20-person check (exit criteria).
> **Deferred:** SLA reminders and escalation, approve from email (FR-PLT-23, 24 — S); HR setting a delegation for someone else; selfie at check-in and anti-fraud heuristics (FR-ATT-05, S); direct device integration (FR-ATT-07, S); late / early penalties (FR-ATT-13, S); business trips with per-diem (FR-ATT-16, S); Google Calendar out-of-office and the leave liability report (FR-LVE-06, 09 — S); the insurance decrease / increase obligation for long-term leave (Phase 3 ops tracker); attendance and leave reports; roster grid and roster import, richer schedule editor (several alternate rules, 3+ segments); map picker for locations; hourly leave as a time-of-day picker; replaying historical entity / department placement and pre-rehire employment in day plans and timesheets; a queue for very wide recomputes (nightly job + HR button today); import files above 5,000 rows must be split; unlocking a period (by design: adjustments); printable monthly timesheet; reminders to managers after N idle days.

**Scope:** FR-ATT-01..04, 06, 08..12, 14, 15, 17, 18 · FR-LVE-01..05, 07, 08 · FR-PLT-20..22 (approval engine full) · PWA shell

| Week | Deliverables |
|---|---|
| 1 | Approval engine v1 complete (multi-step, rule-based approvers, conditions, delegate, bulk approve); working calendars, holidays, schedules and shifts, **untracked working days (Saturday WFH auto-credit, FR-ATT-17)** |
| 2 | Leave: types, policies, entitlement and accrual engine (pure, tested), ledger, request flow, team calendar |
| 3 | Check-in/out PWA flow (GPS geofence, IP/Wi-Fi rules, flagging), installable PWA, push notifications, "who's in today" |
| 4 | Device log import with mapping profiles; merge rules; daily timesheet engine (pure, tested: late/early/missing/OT/overnight shifts) |
| 5 | Correction, WFH/off-site, OT and **holiday/rest-day work requests (FR-ATT-18)**; monthly timesheet confirm → approve → lock; HR anomaly console; UAT with the pilot department |

**Exit criteria**
- The pilot department runs a full month; the locked timesheet matches HR's manual check for a sample of at least 20 people.
- The leave-balance ledger reconciles with imported opening balances.
- Check-in p95 is under 3 s during the morning peak.

Rollout: pilot department (2 weeks), then pilot entity, then the group. Direct device API integration (FR-ATT-07) is scheduled after the device models are confirmed (Q5). File import covers the gap.

### Phase 3 — Work management + Operations & compliance tracker (5 weeks) → **M3**

> **Status, 2026-09-20 — all five weeks built on local branch `mvp` (not pushed, not merged, not deployed); `pnpm check` (668 tests) and `pnpm build` pass; migrations 0028–0033, all additive (new tables, nullable columns, no enum change). New RBAC permissions: `work:manage` (c_level, entity_director, department_head), `ops:manage` (hr_admin, hr_staff, payroll, finance), `ops:read` (c_level, entity_director, auditor); policy tests added and `docs/permission-matrix.md` regenerated. None of the exit criteria that need people can be met without the owner (two teams running real work for two weeks; the library reviewed by the chief accountant and the HR lead); the measurable one is met locally: the board's move action on a production build against the local database — median 25 ms, p95 31 ms over 40 calls (criterion: under 300 ms).**
> **Done — task engine v2 (ADR-10 kept, `src/modules/platform/tasks-engine/`):** still one `task` table; kinds `checklist`, `work`, `obligation`. `task` gained `start_date`, `estimate_minutes`, `requester_person_id`; kind-specific data sits in 1:1 extension tables owned by the feature module (`work_task`, `obligation_instance`) — the platform never references feature tables. The 4-value status enum is unchanged: a team's workflow states carry the six work categories and map onto it. The generic `task.status` / `task.reassign` actions now act on checklists only; work tasks and obligations move through their own module (workflow consistency, evidence-required completion). Templates gained `owner_id`, `description`, item `parent_item_id`, `role_key`, `estimate_minutes` and the assignee rule `role:<key>`; work purposes are hidden from and refused by the HR checklist screens. **`/tasks` is now My work (FR-WRK-06):** reviews waiting for me, approvals waiting for me, work tasks, compliance obligations and checklist steps in one inbox, overdue first; the navigation badge sums them.
> **Done — work management (`src/modules/work/`, FR-WRK-01..03, 05..11, 17, 18; S items 12 partly, 13, 16):** work teams (separate from org teams: key, entity or group, optional department, leads and members) each owning one workflow of ordered states (content preset *Brief → … → Reported* or a simple preset); clients and brands (two levels, lightweight, CRM-promotable); labels; projects (one owning team, members from anywhere, client, lead, dates, status) with **privacy** — open to the entity / team-only / private to members and the team's leads; tasks with per-team numbers (`VID-123`), one assignee + collaborators, requester, priority, start / due, estimate, labels, client, channel, content format, checklist, https links (Drive links are plain URLs), sub-tasks, dependencies with cycle guards, every field change in `work_activity`. **Views:** list with in-memory instant filters kept in the URL, grouping, saved filters (personal / shared), optimistic quick-create; kanban board (native drag and drop, optimistic with snap-back and reason, keyboard / phone fallback); project calendar and a cross-project content calendar with days off shaded and drag-to-reschedule; command palette (Cmd/Ctrl+K, "c" = quick create) with read-only search route handlers. **Conversation:** one level of threaded comments, @mentions (a mention of someone who cannot see the task becomes plain text), reactions, edit / soft delete, attachments through the files module, followers (notifications, never rights). **Review step:** deliverable versions (file or link) → reviewer (task's reviewer → project lead → team lead, never the submitter) approves or requests changes with a comment; revision rounds counted; the task moves through the workflow accordingly. **Templates:** task and project templates with a two-level tree, relative due dates from a start or end anchor skipping days off, roles mapped to people at creation; five starters in `pnpm db:seed` (monthly social retainer, video/TVC production, KOL campaign, event, single post). **Recurring tasks:** pure golden-tested rule engine (daily / weekly on weekdays / monthly on a day or the last day / every n) + the `work-recurring` midnight job, each occurrence made once. **Leader view** by person with overdue and at-risk flags and a one-click nudge (once per task per day). **Workload** (six weeks, open tasks and estimated hours against 40 h less leave and holidays; only the fact of being away is shown, never the leave type). **Intake forms** per team (audience entity or group) → a backlog task with the requester set. **Notifications** (category "Tasks"): assigned, mentioned, commented, status changed for followers, due soon, overdue, review requested / decided, nudge, intake submitted; the `work-reminders` morning job is idempotent per person, kind and day; all of it lands in the existing daily digest.
> **Done — ops tracker (`src/modules/ops/`, FR-OPS-01..05, 07..09; FR-OPS-10 in part):** obligation templates (category, authority, recurrence, due rule, shift rule, applicable entities, owner and reviewer rules, checklist, guidance, links, reminder lead times, escalation days, evidence requirements, penalty note, review status); pure golden-tested **due-rule engine** (periods monthly / quarterly / semi-annual / annual; `after_period`, `in_period`, `after_event`; "last day"; next / previous working day against Sat + Sun and the Phase 2 calendar through `getDaysOff` — Tết 2027 cases included) and status engine (upcoming / due soon / overdue / done / done late); **scheduler** per template × entity × period, idempotent, 100 days ahead, owner and reviewer resolved the task-engine way (the entity's own holder first, never the owners' `*`, never the subject, reviewer ≠ owner); **event-driven obligations pulled from `lifecycle_event`** through a read-only core-hr export: hire / rehire → register social insurance, labour contract, tax code, dependents; termination → close the insurance book, PIT certificate, final settlement; long-term leave → insurance decrease and claim (**closes the Phase 2 deferral**), return → insurance increase; salary change → insurance adjustment; a cancelled event cancels its open obligations; the **monthly payroll calendar** as four templates (lock by the 2nd → propose by the 3rd → CEO signs by the 4th, next working day; pay on the 5th, previous working day; payslips) — the timesheet-lock obligation closes by itself once attendance reports the month locked. **Evidence-required completion:** closing is refused with the list of missing evidence (file, reference number, submitted date, amount in integer VND) and unticked steps; reopen and "not applicable" need `ops:manage` and a reason; evidence of a closed item cannot be removed. **Screens:** `/ops` entity × month dashboard (worst colour per cell, filters by authority / category / owner), `/ops/list`, `/ops/calendar`, `/ops/obligations/[id]`, `/ops/templates` (library; "unreviewed" badge on every instance of an unreviewed template; any edit resets the review), `/ops/history` (archive per template × entity × year with evidence, audited CSV export). **Reminders and escalation:** pure engine + `ops-reminders` morning job, idempotent through `obligation_notice_sent` — lead-time reminders to the owner, overdue to owner + reviewer, then the owner's department head (else line manager), then finance / C-level over the entity and the owners. **Starter library:** 46 templates covering every item of FR-OPS-03 plus the event-driven set, seeded by `pnpm db:seed` as **unreviewed**, each ending "Bản nháp — cần … xác nhận"; re-seeding never overwrites an edited or reviewed template. Notification category "Compliance" (`ops.assigned`, `ops.reminder`, `ops.overdue`, `ops.escalated`).
> **Decisions:** cross-module reactions are **pulled, not pushed** — `lifecycle_event` rows are written in the HR transaction and are the durable event log, so the ops scheduler reads them idempotently (both cron schedules + "Sync now"); core-hr never imports ops and the ADR-08 outbox was not needed. The owner's `*` and `work:manage` do **not** open private projects (private means members and the team's leads; the audit log still records everything). Viewing ≠ contributing: an entity-visible project is read-only for non-members; collaborators (workforce type) see only projects they belong to and never get the company directory in pickers. A project belongs to one team and uses its workflow; a task keeps its team. Following never grants access. Remote images and videos are never embedded (CSP) — links only; task links must be https. The audit log records that someone commented and who was mentioned, not the words. Authorities' weekends are Saturday + Sunday; fiscal year = calendar year. Evidence files are `public_internal` tier gated by the ops policy (readers: `ops:read` / `ops:manage` in the entity, the assignee, the reviewer). The dashboard and archive are for ops readers; an assignee without a role works from My work. Intake forms choose their audience because the studios serve sister entities. The workload counts hours of tasks in projects the lead cannot open, without titles.
> **Verified locally** over HTTP with forged sessions as owner, CEO (c_level), finance, entity HR staff, department heads / team leads, members, a member of another team and plain employees — every new action's allowed and refused paths with audit rows incl. `.denied`: private project 404 and absent from search even for the owner; board / calendar / comment / mention / reaction / follow / upload → open → remove; submit → self-review refused → changes requested → resubmit → approve; template → project with roles; recurrence and every cron endpoint run twice (second run creates / sends nothing; 401 without the secret); nudge twice; obligations per persona (entity HR sees its entity only, employee 404), completion refused without evidence → evidence → complete → reopen with a reason; HR event → obligation → event cancelled → obligation cancelled; dashboard, calendar, archive and export scoping; reminders and escalation wording in Vietnamese; workload without any leave type in the HTML; intake across entities. **Tests (668):** golden tests for graph / rank, filter, board, calendar grid, mentions, reminders, review, template tree, recurrence (month end, leap year), risk, workload, intake, due rules, status, escalation and dashboard engines; policy tests for both modules; PGlite tests keeping the SQL visibility filters in step with the pure policies for every kind of viewer, plus comments, followers, reminders, reviews, templates, recurrences, scheduler idempotency, entity applicability, real-calendar shifting, event pull and cancel, evidence refusal, reminder idempotency and escalation recipients, dashboard scoping and intake visibility.
> **Demo data** (verified from an empty database): `pnpm db:migrate && pnpm db:seed && pnpm db:seed:demo`, then with `pnpm dev` running `pnpm db:seed:demo:ops && pnpm db:recompute`. Two creative teams (Video Production at SZM, Creative & Social at SZC), four clients / brands, six projects (one private, one generated from the retainer template), about 90 tasks with sub-tasks, dependencies, comments, mentions, a deliverable in its third version, two recurrences, two intake forms with requests, estimates and approved leave for the workload view; 199 obligation instances from August to December 2026 for the three demo entities (61 closed with evidence, one late, three overdue and escalated, six in progress, the rest upcoming; 34 from HR events). The ops demo seed goes through `/api/cron/ops-backfill` because `tsx` cannot load the `server-only` scheduler modules.
> **Not yet verified:** anything in a real browser or on a phone — drag and drop on the board and calendar, the command palette keys, the @-mention picker, optimistic rows, file pickers, CSV download, the dashboard grid on a small screen; email and push delivery of the new notification kinds; the list view with several thousand tasks (filters run in the browser over one project's tasks); the cron triggers on Vercel.
> **Needs the owner:** `pnpm db:seed` on production (starter work templates, the obligation library) and role grants for the new permissions where the catalogue defaults do not fit; **the chief accountant and the HR lead must review every library template** (due rules, authorities, evidence, which VAT / PIT variant applies per entity — monthly VAT and quarterly PIT are switched on, their twins, invoice usage, independent audit and work-permit renewals are off) and the owner approves, then each is marked reviewed on `/ops/templates`; confirm the payroll-calendar days and who signs; HR confirms the 2026–27 holiday rows (they shift due dates); name the people behind owner / reviewer rules per entity where the role-based default picks the wrong person; create the real teams, workflows, clients and project templates with two pilot teams and run them for two weeks (exit criterion); decide whether `work:manage` holders or the owner should be able to open private projects (today: no).
> **Deferred:** custom fields (FR-WRK-04), timeline / Gantt and table views, bulk edit, time logging, cycles, project dashboards (FR-WRK-14, 15, 19 — planned as incremental releases); moving a task between teams; per-field inline editing on the task page; multi-day bars and cross-month drag on the calendar; editing a template item or a recurrence rule in place, templates deeper than two levels, project-less recurrences; filters on the leader view (caps at 1,000 tasks); reminders to collaborators; maker-checker completion (FR-OPS-06 — the reviewer is stored and notified, nothing more); full payroll-run linkage (FR-OPS-10 beyond the timesheet lock, with Phase 5); detecting an early return from long leave for the insurance-increase obligation; KB procedure links are plain URLs until Phase 4; deleting templates (switch off only); an ops demo seed that does not need the dev server.

**Scope:** FR-WRK-01..03, 05 (list/board/calendar), 06..11, 17, 18 · FR-OPS-01..05, 07..09 · then S items as time allows (FR-WRK-04, 12, 13, 16; FR-OPS-06, 10)

| Week | Deliverables |
|---|---|
| 1 | Task engine v2: teams, projects, tasks/sub-tasks, custom workflows, labels, client/brand, priorities, dependencies; list view with fast filtering; quick-create and command palette |
| 2 | Board (drag and drop, optimistic updates), calendar view (content calendar), comments/mentions/activity, attachments + Drive links, notifications and digest |
| 3 | Review/approval step with deliverable versions; task and project templates (seed: monthly social retainer, video production, KOL campaign, event); recurring tasks; **My work** inbox; leader view |
| 4 | Ops tracker: obligation templates with due-date rules and holiday shifting, per-entity instance scheduler, evidence-required completion, event-driven obligations from HR events (incl. the monthly payroll calendar: lock by the 2nd → propose by the 3rd → CEO signs by the 4th → pay on the 5th), seed library workshop with the chief accountant and HR lead |
| 5 | Compliance calendar and dashboard (entity × month), escalation chain, history/archive; workload view with leave overlay; intake forms; UAT with two creative teams + finance |

**Exit criteria**
- Two creative teams and the HR/finance team run their real work in the system for two weeks.
- Common task interactions feel instant (optimistic UI; p95 server round-trip under 300 ms).
- The obligation library has been reviewed by the chief accountant and the HR lead, approved by the owner, and the next three months of instances have been generated for every entity.

Timeline/Gantt, time logging, cycles and project dashboards follow as incremental releases during phases 4–6.

### Phase 3.5 — OKRs & KPIs (2 weeks) → **M3.5**

> **Status, 2026-09-20 — both weeks built on local branch `mvp` (not pushed, not merged, not deployed); `pnpm check` (742 tests) and `pnpm build` pass; migrations 0034–0036, all additive (new tables, triggers, one partial unique index). New RBAC permissions: `performance:manage` (hr_admin, hr_staff), `performance:goals` (c_level, entity_director, department_head), `performance:read` (those three + auditor); policy tests added and `docs/permission-matrix.md` regenerated. Neither exit criterion can be met without the owner: they need the real 2027 goals and HR's manual check of the first closed month.**
> **Done — OKRs (`src/modules/performance/`, FR-PRF-01):** `goal` tree group → entity → department → team → individual with a parent link (higher-or-equal level, same year, annual or same quarter, no cycles), annual and quarterly periods (`2027`, `2027-Q1`), status draft / active / closed / cancelled; `key_result` with metric types number, percent (integer hundredths), currency (integer VND) and milestone (checklist), direction implied by start → target, weights; **append-only weekly check-ins** (`goal_check_in`, database trigger refuses update and delete) with value, confidence (on track / at risk / off track) and a note — the latest sets the key result's current value. **Pure progress engine** (golden-tested, integer basis points, BigInt ratios, round half up, clamped 0–100 %): a goal with key results = their weighted average; a goal without = the weighted average of its active children; draft and cancelled children and children with nothing to measure are left out; confidence = the worst below; explanation trace; staleness helper (no check-in for 7 days). **Closing freezes the figure** (`final_progress_bp`) — nobody closes their own individual goal (a manager above them or HR does), reopening is HR only with a reason; a key result with check-ins cannot be removed or change its metric type. Screens: `/performance` (my goals, quick check-in, company goals), `/performance/goals` (**alignment view** for a year / period, unaligned goals listed apart), `/performance/goals/new`, `/performance/goals/[goalId]` (key results, check-in history, progress trace, edit, activate, close, cancel, re-parent).
> **Done — KPIs (FR-PRF-02):** KPI library (`kpi_definition`: unit, higher / lower is better, monthly / quarterly, attainment cap 120 % and floor by default; 17 starters for marketing, HR, finance and account roles in `pnpm db:seed`, re-seeding only adds missing codes); **templates per position** (`position_kpi`, weights and targets; an entity's own set replaces the group's); **month-dated assignments** per person (`kpi_assignment`; "apply the template to the holders" is idempotent; overlaps refused); **actuals** (`kpi_actual`, value or "not applicable" with a required note) entered on a grid by a manager in the person's chain or by HR, **and imported** on the import framework (`kpi_actuals`: employee code, KPI code, period, actual, note; people outside the importer's reach look non-existent; unknown KPI, no assignment, wrong period kind, closed month and duplicates are reported; re-import of an open month overwrites and is idempotent). **Pure score engine with an explanation trace** (golden-tested): attainment = actual / target (or target / actual when lower is better), floor, cap, month score = Σ weight × attainment / Σ weight over the lines due that month; a quarterly KPI is scored in its quarter's last month; the annual figure is Σ(weight × months covered × attainment) / Σ(weight × months covered) **computed only from stored snapshots**. **Close / lock:** HR closes a month per entity once it is over; refused with the list of blockers (a due KPI with neither an actual nor "not applicable") unless overridden with a reason — then a missing line scores 0 and is flagged; the close stores one `kpi_score` row per person (score, full trace with every input, inputs hash, revision). Stored scores are **immutable by database trigger** (never deleted; the only update is marking a row superseded, once; one current row per person and month by partial unique index); nothing in a closed month can be added or edited (actual, assignment, weight, target); reopening takes a group-wide `performance:manage` and a reason, is audited, and the next close writes revision n + 1 beside the superseded row. Open months show a clearly labelled provisional score from the same engine. Screens: `/performance/kpis` (my scorecard, trace, year to date), `/performance/kpis/[personId]`, `/performance/team` (**manager dashboard**: month score, missing actuals, OKR progress, worst confidence, stale check-ins), `/performance/team/actuals`, `/performance/overview` (**owner dashboard**: per entity and department — people scored, average and spread, close status per month, top-level OKR progress; scoped to the viewer's reach), `/performance/admin/{library,positions,assignments,periods,import}`.
> **API for Phase 8 (`@/modules/performance/service`, no authorization inside — the caller decides):** `getOkrResults({ personId, year })` → the person's individual goals and the top-most goals of their team / department / entity / the group, each with `progressBp` and whether the figure is final (frozen); `getKpiResults({ personId, year })` → `{ scoreBp, closedMonths, openMonths, months: [{ month, scoreBp, revision, scoreId, closedAt }], final, byKpi }` from current stored scores only; `getPerformanceResults({ personId, year })` = both; `isKpiMonthClosed(entityId, month)`. **Phase 8 must refuse `reopenMonth` once a bonus run has used the month** (marked in the code) and should store the `scoreId`s it used.
> **Decisions:** unit goals (group to team) are `public_internal` — every non-collaborator sees the alignment tree; **individual goals, KPI targets, actuals and scores are personal tier**: read by the person, every manager in their chain above (direct or skip-level, FR-PRF-08), holders of `performance:read` / `performance:manage` whose scope covers the person — never colleagues, and not compensation tier (line managers must see and enter them; the bonus amount derived in Phase 8 is compensation). In a unit's progress trace, lines of goals the viewer may not read lose their identity and figure. Nobody enters their own actuals or sets their own weights and targets — not HR, not the owner. The management chain is walked in memory from one directory read. Only months are closed; the score period is the month. A provisional score leaves missing lines out; an overridden close scores them zero. Assignment overlap is checked in the use-case, not by a database constraint. core-hr gained the read-only export `listPositionHolders(onDate)`.
> **Verified locally** over HTTP with forged sessions as owner, group HR admin, entity HR staff, department head / line manager, employees, a probationer, a colleague and another entity's head — pages per persona (404 for a colleague's or another department's goal and scorecard, 307 signed out), every action's allowed and refused paths with audit rows incl. `.denied`: goals per level and scope, key results, check-ins, close (refused for oneself, progress frozen), reopen rights; actuals refused for oneself, a colleague, another entity's HR, a closed month; close forbidden / blocked with details / month not over / override; edit after close refused; reopen → revision 2, and **a reopen and re-close reproduced the seeded scores and input hashes exactly**; a direct SQL update of a stored score is rejected; template apply twice; import stage → commit → same file again changes nothing. **Demo data** (verified from an empty database: `pnpm db:migrate && pnpm db:seed && pnpm db:seed:demo`): 31 goals, 53 key results, 79 check-ins — a 2026 set with Q3 check-ins of mixed confidence and a closed Q2 goal, and the 2027 group → SZM / SZC → department → individual tree; 39 template lines, 45 assignments covering **every demo person** from July 2026 into 2027, 77 actuals; July 2026 closed for SZG, SZM and SZC, August for SZM and SZC (SZC with an override over one missing actual), SZG's August blocked by a missing actual, September open — 24 stored scores.
> **Not yet verified:** anything in a real browser (forms, confirm dialogs, the entry grid on a phone); the English rendering; an `.xlsx` actuals file; a collaborator's session (unit-tested only — the demo collaborator has no mailbox).
> **Needs the owner:** `pnpm db:seed` on production (starter KPI library) and role grants where the catalogue defaults do not fit; the real 2027 group, entity and department OKRs and each person's goals or KPIs (exit criterion) — the starter KPI definitions, caps, floors, weights and targets are placeholders for HR and the department heads to set; HR to check the first closed month's scores against their manual calculation (exit criterion); confirm the rules chosen here — missing actual at an overridden close scores 0, attainment capped at 120 %, lower-is-better scored as target / actual, annual figure weighted by months covered, nobody (not even the owner) enters their own actuals, individual goals hidden from colleagues.
> **Deferred:** reminders and notifications (weekly check-in, missing actuals before the close); an HR approval step for actuals; per-month target overrides; editing a template line in place (saving it again updates it); CSV export of scores; key-result reordering; team-level goals in the demo (no org teams are seeded); a "show cancelled" toggle in the alignment view (`?cancelled=1` works); review cycles, the final yearly result and the bonus scheme (Phase 8, FR-PRF-03, 09, FR-PAY-21).

**Scope:** FR-PRF-01, 02 (moved forward; see the note in §1)

| Week | Deliverables |
|---|---|
| 1 | Goal tree (group → entity → department → team → individual), key results with metric types, weekly check-ins with confidence, progress roll-up, alignment view |
| 2 | KPI library per position with weights and targets, monthly/quarterly actuals entry (by manager or HR, with import), computed KPI score per person per period, manager and owner dashboards |

**Exit criteria**
- 2027 company and department OKRs are entered, and every employee has individual goals or KPIs assigned.
- The KPI score for the first closed month is computed and checked by HR against their manual calculation.

### Phase 4 — Knowledge base + internal comms (3 weeks) → **M4**

> **Status, 2026-09-20 — all three weeks built on local branch `mvp` (not pushed, not merged, not deployed); `pnpm check` (857 tests) and `pnpm build` pass; migrations 0037–0043, all additive (new tables, triggers, a generated column, nullable columns). New RBAC permissions: `kb:manage` (hr_admin, hr_staff), `comms:manage` (c_level, entity_director, hr_admin, hr_staff, department_head); policy tests added and `docs/permission-matrix.md` regenerated. New dependencies: Tiptap 3 (`@tiptap/*` 3.31, React 19 peer range), `marked`, `mammoth`. No exit criterion can be met without the owner and HR: the real handbook and SOPs have to be written or imported, and acknowledgement switched on for them.**
> **Done — knowledge base (`src/modules/kb/`, FR-KB-01..07, 09, 10):** spaces (`open` / `controlled`, group-wide or of one entity) with a **page tree**; a page has a working copy and **immutable published versions** (`kb_page_version`, database trigger refuses update and delete); readers see the published version, editors the draft. **Block editor: Tiptap 3**, content stored as ProseMirror JSON plus derived plain text — headings, lists, tables, callouts, code, links, images and file attachments (platform files module, behind a permission-checked route: a reader fetches only files the published version shows), embeds (YouTube, Google Drive / Docs, Figma, Canva through a pure allow-list that builds the iframe address). **No HTML is ever rendered**: the reading view is a server-side React renderer of the JSON, every save passes a pure validator of allowed nodes, marks, attributes and link schemes (`javascript:` links, unknown nodes and foreign embeds are refused). **Permissions per space and per page** (`kb_access`, one `subject_key` column: `all` / `entity:` / `department:` / `team:` / `role:` / `person:`, level view or edit; a page-level restriction is inherited by its subtree through a denormalised `access_root_id` that follows a move); every list, search and feed query filters **in SQL** and a test keeps the SQL filter in step with the pure policy; collaborators see only what names them. **Version history** with line diff and restore. **Publish workflow** for controlled spaces on the approval engine (`kb_publish`: the space's KB managers are asked; working copy frozen while in review; approve publishes as the author's revision; return → resubmit on the same request; reject / withdraw give the page back; review page shows the diff against the published version). **Policy acknowledgement**: must-read pages with an audience matched in SQL (new joiners owe it automatically), append-only confirmations, **re-acknowledgement only after a major revision**, first notice + reminders every three days with overdue wording (`kb-ack-reminders` job, at most one a day per person), "remind now", completion report by entity and department with CSV, "my acknowledgements"; page owner and review-by date with a stale-content reminder. **Search**: Postgres full-text search (`simple` configuration, generated `tsvector`, GIN index) over accent-stripped text — "nghi phep" finds "Nghỉ phép" —, prefix match on the last word, title-weighted ranking, snippets that keep their accents, space filter; recently viewed, popular (30 days), recently updated. **Templates** (`kb_template`, six Vietnamese starters in `pnpm db:seed`; save a page as a template). **Import**: pasted Markdown, `.md` and `.docx` (mammoth → pure HTML-to-Markdown → the same pure `markdownToDoc`) into a draft page — the Google Docs path is "download as Markdown or Word", no Google API. **For the Phase 9 assistant:** `kb_page_chunk` rebuilt inside every publish (pure heading-aware chunker; unchanged passages keep their vector), embeddings adapter (`EMBEDDINGS_API_KEY` / `EMBEDDINGS_MODEL`; Voyage AI driver written, never run; without a key a deterministic feature-hashing fake, `fake-hash-256`), `kb-embeddings` job, and in `@/modules/kb/service`: `searchKb(viewer, …)` and `retrieveKbChunks(viewer, { query, limit, spaceId? })` — both permission-filtered in SQL (`kbViewerOf(user)` builds the viewer).
> **Done — internal comms (`src/modules/comms/`, FR-COM-01..03):** **announcements** — plain text, optional KB page link, audience by subject key (all staff / entity / department / team / branch / person) matched in SQL both ways, posting takes `comms:manage` over **every** target (a department head reaches their department only), pinned, **scheduled** (visibility computed from `publish_at` on read, so timing is exact; the `comms-announcements` job notifies a scheduled audience once), expiry, **read tracking**, optional must-acknowledge, read report by department; `/announcements`, `/announcements/[id]`, `/announcements/manage`. **Home feed** replaces the placeholder `/home`: what waits for me (policy acknowledgements, announcements to confirm, approvals), announcements, new joiners (14 days), **birthdays as day and month only — the year is never selected from the database** (`listStaffOccasionFacts` in core-hr), work anniversaries in whole years, kudos, new KB pages (through the KB's own permission filter); collaborators get nothing about people; the date-window logic is a pure engine with golden tests (29 February, year wrap). **Kudos** tied to company values (`company_value` is configuration, five placeholders in `pnpm db:seed`), never to oneself, removable by the sender or a covering `comms:manage` holder; `/kudos`. **Onboarding checklists link to KB pages**: `link_url` on `task_template_item` and `task` (in-app path or https only; the platform stores an address and knows nothing of the KB), shown as "Đọc hướng dẫn" on task lists. Notification categories `kb` and `comms`.
> **Decisions:** Tiptap over BlockNote / Lexical (maintained, React 19, plain JSON, custom nodes are small). Accent-insensitive search stores `toSearchKey()` text (the convention `person.search_name` already uses) instead of calling the `unaccent` extension: `unaccent()` is not immutable, so it cannot back a generated column or index without a wrapper, and it lives in a different schema on Supabase than on plain Postgres / PGlite — same result, no extension. **Vectors are `real[]` for now**: pgvector 0.8.2 is available in the local Supabase image but not in PGlite (tests), and the dimension depends on a model nobody has chosen; Phase 9 adds `vector(n)` + HNSW when it re-embeds with the real model — `retrieveKbChunks` (cosine in the application over at most 2,000 permission-filtered candidates) is the seam. A published page stays readable while its next revision is in review. A page-level `edit` row makes a reader of the space the editor of that subtree. `kb_publish` is never bulk-approvable. Acknowledgement and announcement audiences take no roles; a named collaborator who cannot open the page is not asked. The acknowledgement due date counts from the later of the day the requirement (or its major revision) took effect and `person.created_at`, plus `ack_due_days` (default 14). Approvals `SubmitInput` gained an optional `target` so a request about no person reaches entity-scoped approvers. Birthdays (day / month) are shown to all staff, as the plan says; an opt-out is deferred. Editing an announcement's audience after publication notifies nobody again.
> **Verified locally** over HTTP with forged sessions as owner, group HR admin, entity HR staff, department head, employees of two entities, finance and a new joiner — pages per persona (404 for spaces, restricted subtrees, drafts, reports and announcements outside one's reach; 307 signed out); every action's allowed and refused paths with audit rows incl. `.denied`: spaces, access rows, pages (save, publish, restore, move, restrict, upload), a `script` node / `javascript:` link / foreign embed refused, a direct SQL update of a published version rejected; submit → return → resubmit → approve, own request and non-assignee refused; acknowledge twice idempotent, outside the audience refused, remind-now then nothing on a second call; search per persona incl. "nghi phep"; Markdown and `.docx` import; page from a template; a department head posting to their department and refused for all staff and another department; scheduling, the announcement job run twice (14 notices, then 0), read tracking; kudos rights; `/home` responses contain no birth year. **Demo data** (from an empty database: `pnpm db:migrate && pnpm db:seed && pnpm db:seed:demo`): five spaces, 22 Vietnamese handbook pages and SOPs (marked as samples), two must-read policies (one half confirmed and overdue for the rest, one reset by a major revision), one revision waiting for review, chunks with fake embeddings, seven announcements, twelve kudos, three birthdays in the week, onboarding and offboarding steps linked to handbook pages, a new joiner (`han.lam@suzu.group`) with a running checklist.
> **Not yet verified:** anything in a real browser — above all **the Tiptap editor itself** (toolbar, tables, uploads, paste) has only been exercised through its actions and a schema test; the English rendering; a collaborator's session (unit-tested only — the demo collaborator has no mailbox); the three-day reminder rhythm and overdue wording over real days (unit-tested); the Voyage embeddings driver; `retrieveKbChunks` over HTTP (no endpoint until Phase 9; covered by tests). One test failed once during a full `pnpm check` of week 3 ("expected function to throw") and could not be reproduced in nine further full runs — watch for a flaky test.
> **Needs the owner:** `pnpm db:seed` on production (KB templates, company values) and role grants for `kb:manage` / `comms:manage` where the catalogue defaults do not fit; the real spaces, the employee handbook and the top 20 SOPs — written in the editor or imported from Google Docs as Markdown / Word (exit criterion; the demo pages are samples only); which policies are must-read, for whom, and the due period (exit criterion); the real company values for kudos; re-adding production onboarding checklist items with their KB links (items cannot be edited in place); an embeddings key (`EMBEDDINGS_API_KEY`, Voyage AI) before Phase 9, then a re-embed; confirm the rules chosen here — birthdays shown to all staff as day / month, re-acknowledgement only on major revisions, reminders every three days, department heads may announce to their own department.
> **Deferred:** pulse surveys (FR-COM-04); kudos points and leaderboard; page feedback, comments and "ask the owner" (FR-KB-08); semantic search in the UI and AI answers (FR-KB-11, Phase 9); learning paths (FR-KB-12); an @mention picker in the editor (the node is accepted and rendered); a screen to switch templates off (the action exists); roles as audiences; a birthday opt-out; open jobs on the feed (Phase 7); ops obligation links pointed at KB pages (they take any URL already); polls, suggestion box, room booking (FR-COM-05..07); removing the unused `home.roadmap` / `home.phase` message keys.

**Scope:** FR-KB-01..06 · FR-COM-01, 02 · S items: FR-KB-07..10, FR-COM-03, 04

| Week | Deliverables |
|---|---|
| 1 | Spaces, page tree, block editor (tables, callouts, embeds, attachments), permissions, version history |
| 2 | Publish workflow for controlled spaces, policy acknowledgement with audience targeting and reminders, Vietnamese-aware full-text search, templates, Google Docs/Markdown import |
| 3 | Announcements (targeted, scheduled, read tracking), home feed (joiners, birthdays, anniversaries, new pages), kudos, pulse survey v1; content migration sprint with HR |

**Exit criteria**
- The employee handbook and the top 20 SOPs are published.
- Policy acknowledgement is tracked for all staff.
- Onboarding checklists link to KB pages.

Embeddings for every page are generated from this phase on, so the AI assistant in Phase 9 starts with a full index.

### Phase 5 — Payroll (6 weeks build + 2 months parallel run) → **M5**

> **Status, 2026-09-20 — all six build weeks on local branch `mvp` (not pushed, not merged, not deployed); `pnpm check` (1,495 tests) and `pnpm build` pass; migrations 0044–0051, all additive (new tables, one enum value `pay_profile_change`, one nullable column on `session`, triggers). No new RBAC permissions — `payroll:read` / `propose` / `approve` / `pay` / `rules` and `rules:propose` were already in the catalogue, so the permission matrix is unchanged. None of the four exit criteria can be met without the owner: the first three need the real Excel files, a real parallel cycle and HR's confirmation; the fourth (a penetration-style review of the compensation endpoints) is **done** as far as this session can do it — `docs/PAYROLL_SECURITY_REVIEW.md`.**
> **Done — compensation setup (`src/modules/payroll/`, FR-PAY-01, 02, 04, 07, 08):** **pay profiles** per employment, effective-dated with an exclusion constraint — Statutory or Simple (D18), the Simple basis (probation / internship / service / short-term / retiree / other) and an optional review date; a first Statutory profile takes effect at once, **Simple and every move between profiles are owner-approved** and written to the timeline as `pay_profile_change`; owner-only exposure report by basis and time on profile (R11). **Pay component catalogue** per entity or group: kind (earning / deduction / employer cost), tax treatment (taxable / exempt / exempt to a cap), subject to insurance, fixed or attendance-prorated, named rounding rule, optional formula; versions go propose (`rules:propose`) → **owner decides** (`payroll:rules`), as do `payroll_policy` (proration basis, union on/off, Simple-profile PIT treatment, variance threshold, pay day) and the statutory parameters — FR-PLT-39 reused, not rebuilt. 30 starter components in `pnpm db:seed`, idempotent. **Safe formula language** (`engine/formula/`): tokenizer → parser → typed check → BigInt evaluator, `min`/`max`/`if`/named rounding, other components readable as `c_<code>`; no `eval`, no `Function`, no member access, no bare division, bounded depth and length, hostile input tested (`constructor`, `__proto__`, deep nesting). **Salary structures** effective-dated and **envelope-encrypted** (`field-contexts.ts`), reached through a `salary_change` request on the approval engine (figures only in `payload_enc`; an approver the org chart names sees no figures and cannot decide; never bulk-approvable; numbered decision document at `/payroll/salaries/decisions/[structureId]`).
> **Done — engine (`src/modules/payroll/engine/`, pure, no I/O, FR-PAY-10..17, 19, 20):** `calculatePerson(input) → PersonPayResult` over named, separately tested stages — timesheet inputs → pro-rating → earnings → overtime → insurance → union → PIT → net. **Insurance** (FR-PAY-11) on the insurance salary with both caps (reference level × multiplier, regional minimum × multiplier) and the edge cases: mid-month joiner and leaver, unpaid leave against the 14-day threshold, probation not covered, past retirement age, foreigner, insured elsewhere, fund-paid maternity and long sick. **PIT** (FR-PAY-13): taxable income less exempt allowances to their caps and the OT/night exemption, less employee insurance, personal and dependent deductions by registered months, through the progressive brackets; **flat withholding** (FR-PAY-14) for non-residents, no-contract / under-three-month / CTV payments above the per-payment threshold, with **commitment-form** exemption. **Retro items** (FR-PAY-17) from late-approved salary changes and post-lock timesheet adjustments (`listAdjustmentsForPayroll` / `markAdjustmentsTaken`), derived by **replaying the stored input** of the original run rather than recalculating — which caught a double-counting bug with bonuses typed into that run. **Off-cycle runs** (FR-PAY-19) taxed with the month's prior run aggregated for the progressive brackets — Phase 8 pays the year-end bonus through `createOffCycleRun`. **Net→gross converter** (FR-PAY-03) at `/payroll/tools/net-to-gross`, exact in integer VND, saying when a net figure has no exact gross. Every line carries its inputs, the rule or formula applied, its rounding rule and the parameter, component, policy and engine versions (FR-PAY-20); a test reproduces a stored result byte for byte. **16 golden fixtures** in `engine/golden/*.json`, auto-discovered, each **synthetic with the arithmetic derived by hand in its `derivation` field** — adding a real anonymised case is dropping in a file (README). **Property tests** (fast-check): net ≤ gross, converter round-trips, PIT monotonic, insurance never above its caps, lines sum to totals.
> **Done — run lifecycle (FR-PAY-30, 31; D17):** draft → calculated → **proposed (HR lead)** → **approved (CEO)** → **payment prepared (chief accountant)** → paid → locked, each guarded by permission over the entity **and** step-up; the CEO returns a run to HR with a reason from *proposed* or *approved* (never once payment is prepared) and the return unsigns what it passes; database triggers (0048) make a locked run, its people and its inputs immutable and `payroll_run_event` append-only. Calculation is the durable **`payroll-calculate`** job with per-person progress, a single-UPDATE claim, and takeover of a claim that stops beating for ten minutes. **Variance** (FR-PAY-31) per person and in total against the previous month: net change beyond the policy threshold, new and removed people, negative net, missing bank account, missing tax code. A run refuses a period whose timesheet is not locked, and a locked payroll period refuses new runs, recalculation and edits. **The Phase 3 ops payroll calendar is closed (FR-OPS-10):** `INT-PAYROLL-PROPOSE`, `INT-PAYROLL-SIGN`, `INT-SALARY-PAYMENT` and `INT-PAYSLIP-RELEASE` close themselves as the run moves, pulled idempotently from `listRunMilestones` (statuses and dates only).
> **Done — outputs (FR-PAY-32..35, 39):** **payslips** released to ESS only after the CEO signs, reading their figures from the run itself; **PDF** with no new dependency — a small writer embedding Roboto cut to the Vietnamese ranges (45 KB, Apache-2.0, recipe in the font README) as CIDFontType2/Identity-H with a ToUnicode map, so the text draws correctly and stays selectable; append-only **payslip query** thread to C&B with no amounts in any notification. **VCB and ACB bulk-transfer files** as separate documented format modules behind a registry, streamed and never stored (only a receipt of who generated what is kept), totals reconciling with the run and anyone the format cannot write reported by name rather than dropped. **Cash payment sheet** for the Simple profile with the accountant's disbursement and the employee's receipt confirmation; **a run cannot be marked paid until every bank batch covers its people and every cash row is handed over**; cash totals reported apart from bank totals. **Six reports** (register, cost by entity and department, insurance summary for the BHXH notice, PIT withholding summary, union report, cost trend), entity-scoped as a WHERE clause; the three that name people take `payroll:propose` — the CEO, the chief accountant and an auditor read totals only; every CSV export audited with what left and for whom, never a figure. **Statutory filing data** (FR-PAY-35): D02-LT from two months' stored results side by side, 05/KK-TNCN monthly or quarterly as indicators plus a working sheet naming the people behind them, 05/QTT-TNCN with appendices 05-1/05-2 and the year's tax recomputed as a check, the dependants register, and per-person withholding certificates built from the same finalization rows. **YTD import** (encrypted, one row per person per year, re-import replaces) carries months the old method paid into the annual finalization. **Parallel run** (FR-PAY-38): reference figures by file or typed, per-person line-by-line reconciliation, each difference classified (system bug / spreadsheet error / rule gap) with a required note — "zero unexplained" needs explanations **and** nobody missing from either side, and an explanation is bound to the difference it was written for, so a recalculation makes it stale rather than silently covering a new gap.
> **Done — step-up re-authentication (FR-PLT-06, open since Phase 0):** `session.reauth_at`, a pure 15-minute freshness rule, `createAction({ stepUp: true })` checked before authorization, `requireStepUp()` for pages, `/step-up`. Behind an adapter: the **Google driver** (own OpenID round trip, `prompt=login`, `max_age=0`, `login_hint`, signed state cookie, nonce, `auth_time` check) is **written and never run**; the local confirm driver lives in `.env.development.local` and `env()` refuses it in a production build or on Vercel (tested). Every compensation screen and payroll action is behind it, own payslips included. **Owner's decision 2026-09-23:** signing in is not a step-up — a new session starts with no `reauth_at`, so the round trip is always asked for, however recent the sign-in.
> **Security review — performed, not described** (`docs/PAYROLL_SECURITY_REVIEW.md`): every read path and all 32 actions enumerated as route/action → permission → test; IDOR probed over HTTP on run, payslip, person, export and cash ids as ten personas (an id that is not yours answers exactly like one that does not exist); the database read for anything legible. **Four findings fixed** — the statutory export action was dead in every case (a `"use server"` file exporting a const answered 500 while its page rendered; build, typecheck, lint and tests all missed it), the two payroll imports were authorized but not behind step-up, Next's dev server **logged salary figures** as server-function arguments (`logging.serverFunctions: false`), and the demo job's production guard was weak. **One accepted:** an out-of-reach `entityId` in a URL falls back to the viewer's own entity instead of refusing (no leak; ~10 lines to fix). Confirmed: every money column is ciphertext, nothing legible in audit, notifications, email, job results or logs, and the **production build** answers `private, no-store` on every compensation screen (Phase 0's open question).
> **Decisions:** payslip and salary file = **self, `payroll:propose` over the entity, and the owner** — line managers, department heads, entity directors, HR staff, finance, the CEO and auditors are all refused, proved by policy tests, by PGlite tests over the list/report/export SQL, and over HTTP. Two levels of sight: `payroll:read` sees totals and the variance list with net per person (what the CEO signs against), only C&B and the owner see a person's full result. C&B locks the period, not the accountant who paid it, so two people close a month. **Insurance is never pro-rated** — one uncovered-days test against the 14-day threshold decides coverage. The pro-rating divisor is the **month's** standard days, not the person's; the insurance base and the OT rate follow the **last** segment of a mid-month change, which splits on the frozen daily rows. Retro is taxed in the month paid and never touches either month's insurance base — a changed base is flagged for the D02-LT export. Off-cycle runs need no locked timesheet, pay no structure lines, make no insurance contribution and credit prior tax without ever refunding. Every structure is born from an approved `salary_change`, initial ones included. `tsconfig` target raised to ES2020 for BigInt literals (type-check only).
> **Verified locally** over HTTP with forged sessions as owner, CEO, chief accountant, group and entity C&B (including one scoped to SZC only), HR admin, HR staff, department head / line manager, employees and a probationer — the **whole lifecycle on demo data**: SZM, SZC and SZG's August 2026 calculated, proposed, signed, paid and locked with payslips out; every step refused for the wrong role with `.denied` audit rows; the CEO's return round trip; a locked period refusing a new run, a recalculation, a step and an off-cycle bonus; the job idempotent, 401 without the secret, a stale claim taken over; a stale session giving 307 to `/step-up` and `step_up_required` with an audit row; PDF download, a payslip query round trip, bank file generation refused for everyone but the accountant, the cash sheet and an employee's receipt, "paid" refused until both settle; every report and export per persona. Repeated greps of `audit_log`, `notification`, `email_outbox`, `approval_*`, `lifecycle_event`, job results and server logs found **no amount anywhere**. **Demo data** (verified from an empty database): `pnpm db:migrate && pnpm db:seed && pnpm db:seed:demo`, then with `pnpm dev` running `pnpm db:recompute && pnpm db:seed:demo:payroll` (and `pnpm db:seed:demo:ops` as before) — pay profiles and encrypted salary structures for all 18 demo people across both profiles (two above the insurance cap, people with dependents, a probationer at 85% who joined mid-August, a collaborator on a service contract, an intern past her review date), three entities' August 2026 locked with 16 payslips and 3 cash receipts, a September draft, one applied and one pending salary change, and a new entity-scoped C&B persona (`ngan.vu@suzu.group`, SZC).
> **Not yet verified:** the golden figures against **real payroll** — every fixture is synthetic and hand-derived from Appendix A (exit criterion 1); a parallel cycle with real spreadsheets (exit criterion 2); the Google step-up round trip; the bank and statutory file layouts against the real templates; anything in a real browser — the payslip PDF was rasterised and looked at, but no form, report grid or download has been used on a phone; the English rendering; `.xlsx` output (everything exports CSV today).
> **Needs the owner:** **the chief accountant must verify every Appendix A value** and the three keys added here (`union.dues_cap`, `insurance.unpaid_leave_threshold`, `pit.overtime_exemption`), all seeded **unverified**; the last three months of real payroll Excel per entity, anonymised into `engine/golden/` (a file each); **confirm every assumed column** — VCB (STT, SO TAI KHOAN, TEN NGUOI HUONG, SO TIEN, NOI DUNG, MA NHAN VIEN), ACB (the same plus beneficiary bank, fee payer "OUR", a TONG CONG total row), D02-LT's change codes TM/TL/GH/KL/OF, and the PIT indicator numbering [21]–[37] (indicator [37], authorised finalization, has no register — HR fills it by hand); the OAuth client needs the redirect URI `<BETTER_AUTH_URL>/api/step-up/callback` before step-up works in production; `pnpm db:seed` on production for the starter components and the group pay policy, and role grants for `payroll` / `finance` / `c_level` where the catalogue defaults do not fit; per entity: proration basis, union on or off, the Simple-profile PIT treatment, the variance threshold and the pay day; each entity's wage region and paying bank account, and each person's bank account and tax code (the variance check lists who is missing one); **confirm the rules chosen here** — insurance never pro-rated, coverage decided by the 14-day test, the month's standard days as divisor, the last segment's insurance base, OT taken as time off in lieu split proportionally across categories (attendance does not record which was taken), and C&B rather than the accountant locking the period. Note for the accountant: a month of unpaid leave **just under** the 14-day threshold still carries a full contribution, so a person can end the month owing the company — that is the law as seeded, and it is flagged, tested and documented.
> **Deferred:** FR-PAY-05 (probation ≥ 85% validated — the demo sets it, nothing enforces it), 06 (salary advance runs), 18 (final settlement on termination), 36 (accounting journal export), 37 (CTV payment runs per job); official `.xlsx` layouts for the bank and statutory files (CSV today); per-pay-component reference figures in the parallel-run import (core figures only); a screen for off-cycle runs (the service is there and Phase 8 drives it); password-protected payslips by email (FR-PAY-32's optional half); the out-of-reach `entityId` URL fallback in the security review; HR's record-only `salary_change` lifecycle event still sits beside payroll's and may double an ops obligation — remove it from `RECORD_ONLY_EVENT_TYPES` when someone confirms nothing else reads it.

**Scope:** FR-PAY-01, 02, 04, 07, 08, 10..17, 19, 20, 30..35, 38 · FR-PLT-39 · then FR-PAY-03 (converter tool), 05, 06, 18, 36, 37. FR-PAY-21 (year-end bonus scheme) is built in Phase 8.

**Pre-work during phases 3–4 (no dev time):** HR supplies the last 3 months of the real payroll Excel files per entity. These become the anonymised **golden test cases**. The owner confirms the pay rules they encode (SRS D17: the owner decides the rules), and Appendix A values are verified with the chief accountant.

| Week | Deliverables |
|---|---|
| 1 | **Pay profiles (Statutory / Simple, SRS D18)**, pay component catalogue, salary structures (effective-dated, encrypted), salary change flow, safe formula language, statutory parameters loaded and signed off |
| 2 | **Payroll engine core** (pure library): timesheet inputs → pro-rating → earnings → insurance (caps, edge cases) → PIT (progressive, dependents by month, exemptions) → net; explanation trace per line; golden tests |
| 3 | Flat-rate PIT cases (CTV, non-resident), OT and holiday-work pay, retro items, off-cycle bonus runs, net→gross converter tool for offers; property-based tests (e.g. net ≤ gross, converter round-trips) |
| 4 | Run lifecycle per SRS D17 — draft → calculated → **proposed (HR lead)** → **approved (CEO)** → **payment prepared (chief accountant)** → paid → locked — as a durable background job with progress; variance checks; payroll calendar targeting payment on the 5th; period locking; owner approval for rule changes (FR-PLT-39) |
| 5 | Payslips (ESS, PDF, payslip query), **VCB and ACB bulk-transfer files**, **cash payment sheet with disbursement and receipt confirmation for the Simple pay profile (FR-PAY-39)**, payroll register and cost reports, insurance and PIT summaries |
| 6 | Statutory exports (insurance increase/decrease data, PIT declaration and finalization data), YTD import, **parallel-run mode with a per-person reconciliation report**, security review of the payroll module |

**Parallel run (2 payroll cycles, overlapping phases 6–7):** the system and the existing method both run. Every difference is investigated and classified as a system bug, a spreadsheet error or a rule gap. Go-live requires one full cycle with **zero unexplained differences** for every entity and sign-off from the owner, with the HR lead and chief accountant confirming the figures.

**Exit criteria**
- The golden tests match the existing Excel payroll to the đồng (or the difference is a confirmed Excel error).
- A parallel cycle completes with zero unexplained differences.
- Payslip access is verified: self, C&B and owner only.
- A penetration-style review of the compensation endpoints is done.

### Phase 6 — Assets & requests (3 weeks) → **M6**

> **Status, 2026-09-20 — all three weeks on local branch `mvp` (not pushed, not merged, not deployed); `pnpm check` (1,794 tests) and `pnpm build` pass; migrations 0052–0059, all additive. No new RBAC permissions: `asset:manage`, `org:manage`, `payroll:pay` and `person:manage` already covered everything, so the permission matrix is unchanged. Three of the four exit criteria are met on demo data and need the owner only to be met on real data; the fourth (HR generates contracts from templates) is built but its wordings are drafts nobody has approved.**
> **Done — the generic request builder (`src/modules/requests/`, FR-REQ-01, 02, 04):** a `request_type` row holds a **form** and its Vietnamese and English names; `genericRequestType(row)` turns it into an ordinary `RequestTypeDefinition` with approval type `request:<code>`, and its flow is a plain group-wide `approval_flow` row — so the inbox, delegation, history, bulk approve and the Phase 2 flow administration all work on a type nobody wrote code for. The composition root (`src/app/(app)/approvals/registry.ts`) keeps its static map and merges the database's types in through `allRequestTypes()`. **`engine/form.ts` is pure**: eleven field types with their validation, conditional visibility that may only look **backwards** (a question nobody was asked decides nothing, and its answer is never stored), a designer-side `formProblems` in the shape of `flowProblems`, and the list of answers a flow condition may test — so "above twenty million also needs the CEO" is configuration. Money is read as Vietnam types it and as the rest of the world does; every refusal is a message key. Screens: `/admin/request-types` (the form designer beside the **existing** `FlowEditor`), `/requests`, `/requests/new[/code]`, `/approvals/request/[id]`. Attachments go through the signed-upload path, owned by the uploader and opened only through the request. `pnpm db:seed` ships the five types the company files — **purchase, payment, confirmation letter, advance, business trip** — each with its flow and thresholds.
> **Done — approve from a notification, Google Chat, the SLA clock (FR-PLT-23, 24, 31):** every approver asked is given a **single-use, short-lived, hashed deep-link token**; `/approvals/act/[token]` is a **shortcut, not a credential** — the session must still be the token's person, and the approval still runs the owning module's own decide action. A token is voided on any move of the request. **Google Chat sits behind an adapter** mirroring the web-push pattern (`chatDriver()`, `chat_delivery` rows, a sweep on the jobs framework): with no `GOOGLE_CHAT_WEBHOOK_URL` the local driver records the delivery and nothing leaves the machine. **SLA** is configurable per request type (remind after N days idle, escalate after M to a manager level), idempotent on `approval_assignee.reminded_at` / `escalated_at`, run by a job on the cron route; the escalation card deliberately carries **no approve button**.
> **Done — the asset register (`src/modules/assets/`, FR-AST-01, 02):** `asset_category` / `asset` / `asset_assignment` / `asset_event` (append-only history), nine screens under `/assets`, digital handover confirmation by the holder, return with a condition note, repair / loss / disposal, transfer, the importer on the shared framework with its template and dry run, 15 starter categories in `pnpm db:seed`. **At most one open assignment per asset is a partial unique index**, proved by inserting past the use-case. **Offboarding collects the equipment**: terminating someone opens a return task per asset due by the last working day, cancelled when the termination is called off.
> **Done — QR labels, hand-rolled (`assets/engine/qr.ts`):** no dependency added — byte mode, ECC level M, versions 1–10, Reed–Solomon over GF(256), eight masks scored by the standard's penalties; a printable A4 label sheet drawn with the Phase 5 PDF writer, the same matrix as inline SVG on the asset page, and a scan landing at `/assets/qr/[token]` where the token is an opaque identifier and the **session still decides**. The **PDF writer moved** from `payroll/exports/pdf/` to `src/modules/platform/pdf/` — the module-boundary rule caught the second module reaching for it, and document generation needs it too.
> **Done — booking, licences, documents (FR-AST-03, 05, FR-CHR-06):** `asset_booking` with a **gist exclusion constraint** on `(asset_id, tstzrange(start_at, end_at, '[)'))` for the three statuses that reserve — a test inserts an overlapping row past the use-case and reads back `23P01` and the constraint's own name; `returned` is outside the constraint so an early return frees the rest of the window. Keepers book into `confirmed`, everyone else **requests** — both hold the slot, so asking early beats confirming late; a bookable asset's facts open to the whole staff, its money does not. **Licence renewals reach the ops tracker the way ops already learns about the world**: assets publishes dated facts and `ops/scheduler.ts` pulls them, exactly as it does for lifecycle events and payroll milestones. Fact id `<licence>:<date>` makes re-sync a no-op; the obligation falls due **14 days before** the money goes out. **Document generation** lives in its own small module `src/modules/documents/` because it spans core-HR, payroll and the PDF writer and belongs in none of them: `document_template` with a sensitivity tier and `{{placeholders}}`, a pure renderer, and a `generated_document` record. Nothing rendered is stored — re-opening re-renders and re-checks **now**.
> **Done — expense claims (FR-REQ-03):** a claim is a request on the builder (same form, flow, inbox, SLA clock, deep links) plus `expense_claim_line`; receipts ride in the submission's attachment list, so the rule that already guards a request's attachments guards them and there is no second rule to get wrong. `engine/expense.ts` is pure: whole positive đồng, a real calendar day (`Date.parse` rolls 2026-02-31 into March, so dates are built and read back), not in the future, not older than 90 days, receipt required above 500,000 ₫. On approval the claim becomes money in the entity's open run through four narrow additions to payroll's barrel (`findOpenRegularRun`, `getRunHandle`, `setRunInput`, `removeRunInput`) that carry ids, month and status — **no amount ever leaves payroll that way**. Finance gets `/requests/claims`, scoped by `entityReach(principal, "payroll:pay")` as a WHERE clause.
> **Decisions:** designing request types is `org:manage`; **filing one is everybody's**; a generic type is **never bulk-approvable** and a code is immutable once requests exist. **Purchase price and supplier are `restricted`** — `asset:manage` over the owning entity; a holder sees their laptop and never its price, and anyone with `person:manage` over a person sees what they hold, which is what makes offboarding work. **Claim lines are `personal`, not compensation** — what a taxi cost is not what anybody earns. **Every document placeholder carries the tier of the fact it reveals, and a body naming one above the template's tier cannot be saved**: the leak has nowhere to live, and the generation and download checks are the second and third lines; context is cut by tier *before* rendering. A refused document answers **exactly like an id that does not exist**. **Expense reimbursement is a new seeded pay component `EXPENSE_REIMBURSE`** — earning, exempt from tax **and** insurance, not prorated, because it refunds the employee's own money. **No open run → the claim waits** and a *sweep* (not a hook on run creation) offers it again, so payroll never has to know expense claims exist; a run past `calculated` keeps the figure it was signed with, and a **cancelled** run frees the stale posting. **Two claims from one person sum into one run line** — `payroll_run_input` is keyed by (run, person, component), so a per-claim line would have overwritten the first. Double payment is refused by a unique constraint, read back as `23505`. The licence lead time (14 days) and the two claim thresholds are seeded configuration, not constants.
> **Three bugs found by using the thing, not by testing it:** a `"use server"` file exporting a `const` makes the module export **nothing** — a 500 page while typecheck, lint, build and 1,532 tests all passed; fixed, and `tests/server-actions.test.ts` now guards every such file repo-wide (the same trap the Phase 5 security review found). The QR **format bits were laid out least-significant-first**, producing symbols that look entirely convincing and that no scanner reads — found by rasterising to PNG and handing them to a real scanner, which found no barcode at all; seven scanner-verified matrices are kept as golden fixtures. And drizzle binds a `Date` fine when it knows the column but has nothing to learn from inside `tstzrange($1,$2)`, where postgres.js throws and PGlite shrugs — all tests green, the page a 500; every instant in a raw fragment now goes through `instant()`.
> **Verified locally** over HTTP with forged sessions as owner, CEO, HR lead, HR officer, chief accountant, entity-scoped C&B, asset keeper, department head / line manager and plain employees — a request filed, approved by the manager **via the deep link**, moved on to finance with a fresh token, with the wrong person and a spent token both refused and audited; a hidden field's answer dropped server-side; a refile round trip after a return; the SLA job 401 without its bearer, reminding once and escalating idempotently; assign → handover → return, a double assignment refused by the database, a label sheet **rasterised and looked at**; a booking conflict; a licence renewal landing in the ops tracker and a second sync creating nothing; the employment-confirmation letter issued by an HR officer and the **salary** one refused to her, to the line manager and to C&B, with 404s indistinguishable from a missing id; an expense claim filed with a receipt, approved, summed into September's draft run (492,000 + 175,000 = 667,000) and impossible to post twice. Repeated greps of `audit_log`, `notification` and `email_outbox` found **no figure anywhere** (audit says "2 khoản"). **Demo data**, verified from an empty database (`drop schema`, all 60 migrations, `pnpm db:seed && pnpm db:seed:demo`): 15 categories and 42 assets — every demo employee's equipment, stock, one in repair, one returned with a condition note — production gear with bookings, two licences with upcoming renewals, six requests at every approval stage, expense claims approved and waiting, and the seeded document templates.
> **Not yet verified:** anything in a real browser or on a phone — the label sheet and the generated letters were rasterised and inspected, but no form or calendar has been touched by a hand; the English rendering; the Google Chat driver (written, never run); label stock and printer alignment; whether the hand-rolled QR reads off matte vinyl at label size rather than off a screen.
> **Needs the owner:** **no HR role carries `asset:manage`** in the catalogue — the demo grants `asset_admin` to the HR officer who hands out laptops, but on the real system that is the owner's call, as is whether finance should see the asset base at all. `GOOGLE_CHAT_WEBHOOK_URL` for the real Chat driver. **The chief accountant confirms the `EXPENSE_REIMBURSE` treatment** (exempt from tax and insurance). Confirm the seeded flows and thresholds (CEO above 20,000,000 ₫, the 90-day claim age limit, the 500,000 ₫ receipt rule, 14 days' licence lead time); `EVT-LICENCE-RENEWAL` is seeded `unreviewed` like the rest of the ops library. **The five document templates and their letterheads are drafts** — they say so in their own text — and no contract or decision wording has been read by anybody qualified; **exit criterion 3 is not met until they are**. Real asset data: an import file of what the company actually owns, with serials and purchase prices.
> **Deferred:** FR-AST-04 (maintenance and incident log — `asset_event` records repair, loss and disposal, but there is no maintenance schedule and no loss/damage → payroll-deduction flow); asset photos (the column exists, no upload screen); an employee opening their own generated letter (self-service goes through the confirmation-letter request type instead); per-category expense caps; `.xlsx` for the asset import template (CSV today).

**Scope:** FR-AST-01, 02 · FR-REQ-01, 02 · FR-CHR-06 · S items: FR-AST-03..05, FR-REQ-03, 04 · FR-PLT-23, 24, 31

| Week | Deliverables |
|---|---|
| 1 | Generic request builder (form designer + flow designer on the approval engine), seed request types, approve-from-notification deep links, Google Chat notifications, SLA reminders and escalation |
| 2 | Asset register, QR labels, assignment/handover/return tied to onboarding/offboarding, import |
| 3 | Production equipment booking calendar, licence/subscription tracking (renewals feed the ops tracker), expense claims, document generation from templates (contracts, decisions, confirmation letters) |

**Exit criteria**
- Every current employee's equipment is recorded.
- Purchase, payment and confirmation-letter requests run in-system.
- HR generates contracts from templates.

### Phase 7 — Recruitment / ATS (4 weeks) → **M7**

> **Status, 2026-09-20 — all four weeks built on local branch `mvp` (not pushed, not merged, not deployed); `pnpm check` (2,130 tests) and `pnpm build` pass; migrations 0060–0065, all additive. No new RBAC permissions and `roles.ts` untouched — `recruit:manage` and the `recruiter` role already existed, so the permission matrix is unchanged. The exit criterion is met on demo data: `pnpm db:seed && pnpm db:seed:demo` produces one opening that ran application → interviews → offer → pre-boarding employee **SZM-0007** with nothing retyped. It is not met on real data, which needs the owner.**
> **Done — the ask, the opening, the pipeline, the candidate database (FR-REC-01, 02, 04):** a hiring request is a request on the **existing** approval engine (type `hiring`, registered at the composition root, never bulk-approvable, flow = line manager → `permission: recruit:manage`); approving it does **not** create the opening — a recruiter does, from `/recruit/openings/new?from=<id>`, which prefills and marks the ask fulfilled. Openings carry an opaque `public_slug`, a hiring team (`job_opening_member`) and a status lifecycle; pipelines are configuration, two seeded (`STANDARD` 8 stages, `SHORT` 5), and every stage declares which rung of the funnel it is so openings with different stage names can still be compared. Duplicate detection is a pure `engine/duplicates.ts`: an identifier match is refused outright, a bare name match once, with the matches in `ActionError.details`.
> **Done — the public careers page (FR-REC-03), the one unauthenticated surface in the product:** `createPublicAction()` sits beside `createAction` and makes parse → rate limit → spam check → run → audit *required fields of the definition*, so a public mutation that forgets its limiter does not compile. The form is **plain HTML posting multipart to a route handler** — no client bundle, works with JavaScript off, POST-redirect-GET. Limits: apply 6/hour, form 60/hour per visitor, where the **visitor is an HMAC of the IP truncated to 16 hex** and the address is never stored or logged. Honeypot plus a 4-second minimum fill time plus a form token signed over the **slug**, so one harvested form cannot be replayed across every job; honeypot and too-fast are **dropped silently and answered like a success**, a stale token is refused out loud because a person can act on it. **No signed upload URL is ever handed to a stranger**: the CV rides in the POST, is refused unread above 5 MB by `content-length`, and is checked in memory against the type allow-list and its own magic bytes. Email templates (invite, reject, offer) go through the **existing** outbox. Kanban board with drag **and** select-and-move, because a drag-only board excludes keyboards and phones.
> **Done — interviews and scorecards (FR-REC-06), built the other way round:** Google Calendar/Meet is unavailable (no keys, no incremental OAuth scopes), so **the `interview` row is the schedule**, `engine/ics.ts` turns it into a calendar file anybody's calendar reads, and `calendar.ts` is an adapter that *may* also push to Google. With no service account the local driver runs and the page says `simulated` out loud rather than pretending. The `.ics` is golden-tested against RFC 5545 — CRLF, folding at 75 **octets** without cutting a codepoint, a stable UID so a reschedule updates rather than duplicates. **Blind feedback lives in `scorecardsFor`, not in a page**: an interviewer sees others' cards only once their own is *submitted*, a draft does not lift the blind, a submitted card cannot be edited, and only somebody in the room may write one. **Being an interviewer is not being on the hiring team** — it admits them to that interview and that candidate's CV and to nothing else. Take-home assignments arrive through the same public machinery, on a link whose **hash alone** is stored.
> **Done — the offer, the letter and the colleague (FR-REC-08, 09):** `job_offer` with a partial unique index (one live offer per application) and a pure state machine. The approval runs on the existing engine (type `offer`, the opening's `department_head` then `permission: payroll:approve`, never bulk-approvable) and **the figure is in two columns and nowhere else** — not in the payload, the summary, a notification, an `application_event` or an audit row. `stepUp: true` on the two actions that write a figure, and the figure on the page and the letter PDF are behind `isStepUpFresh`. The letter is made from `src/modules/documents/`'s own renderer and **nothing rendered is stored**, so re-opening re-checks the reader now. Conversion hands off twice and writes nothing itself: `hireInTransaction` for the person (future start date → `preboarding`, Phase 1's roll-over does the rest) and payroll's `submitSalaryChange` for the figure.
> **Done — referrals, reports, retention (FR-REC-10, 11, 13):** a referral is an ordinary candidate and application — same duplicate check, same clock — plus a row saying who put the name forward. Bonus eligibility is **read from the facts** and never stored. The funnel rests on week 1's decision that a rejection keeps its stage, so an application's stage is also how far it got. The retention job **anonymises rather than deletes**, so the funnel still counts 14 applications and none of them is a person any more.
> **Decisions:** **`roles.ts` untouched.** Candidate data opens to a holder of `recruit:manage` covering the opening **plus the opening's hiring team rows** — that is how a department head sees their opening and nothing else, and it is a row rather than a role. **Recruitment money** (band, expectation, offer amount) needs a covering grant whose `maxTier` is `compensation`, so **`hr_staff` runs the whole pipeline and never sees a figure**; `canReadOfferMoney` additionally admits `payroll:approve`, because an approver who cannot read what they are approving is a bug, not a safeguard. **The candidate does not answer an offer over the public surface** — a recruiter records it; a link to a salary sitting in a mailbox is a hazard nobody asked for. **Converting needs `person:manage` as well as recruitment**: a recruiter who runs every opening still cannot create an employee. **Referring is everybody's**, and a referrer is told the same thing whether the person is new, already on file, or already referred by a colleague — any other answer makes the form a lookup into the candidate database. **Anonymising keeps scorecard ratings and drops their free text**: a 1-to-4 with no name attached says nothing about anybody and is the only record of how the decision was reached. **Consent is versioned** (`CONSENT_VERSION`, bump it whenever the notice changes) and **talent-pool consent is asked separately** from the privacy notice, because agreeing to be considered is not agreeing to be kept. **`/careers` is indexable; the form and the thank-you are not.**
> **Six bugs found by using it, not by testing it:** a **drizzle column embedded in a `sql` fragment renders unqualified** (`"id"`, not `"candidate"."id"`), so inside a correlated subquery Postgres resolves it against the *subquery's own* table — the retention job's "is anybody still being considered?" count was always zero, which would have anonymised live candidates, with typecheck, lint and build all green. **`purgePublicHits` bound a `Date` inside a raw `sql` fragment**, which postgres.js throws on and PGlite shrugs at: the job failed the first time it was ever actually called, a week after its tests went green. The **funnel report was gated on `report:read` alone**, shutting the recruiter out of their own numbers. The first `createPublicAction` **swallowed every expected refusal into a generic failure**, so an applicant was told to try again at a job that had closed. Naming the filer as an offer request's subject made the **wrong department** sign off. And the **CEO had the offer in his inbox and a 404 on the offer itself**. Also: duplicate *certainty* was derived from a similarity score, and two count subqueries sharing a column alias failed at run time only.
> **Verified locally** over HTTP with forged sessions across every persona — owner, CEO, HR lead, HR officer, chief accountant, C&B, recruiter, department heads on and off the hiring team, interviewers with no recruitment role, and plain employees — and anonymously for the public surface. **Anonymously:** an honest application with a real PDF end to end; honeypot, instant submit, forged and stale tokens, missing consent, an executable renamed `cv.pdf`, a 6 MB upload, an `.svg`, and the seventh apply in an hour all refused or silently dropped with nothing written; a repeat application **byte-identical** to a first; XSS stored verbatim and rendered escaped; a draft slug, an invented slug and an opening's own uuid all 404 alike; **no uuid anywhere on the public pages**; audit rows carrying the hashed visitor and no name, address or IP. **With sessions:** a hiring request approved and turned into an opening prefilled from the ask; a band posted by hand and dropped for `hr_staff`; a hiring manager seeing exactly one opening and 404ing on everything else; **Huy submitted → Chi saw nothing → Chi's draft still nothing → Chi submitted → both saw both**; an `.ics` downloaded, unfolded and structurally validated; an offer approved by a hiring manager **who never saw a figure** and accepted by a recruiter who never saw one either; conversion to **pre-boarding `SZM-0007`** with eight onboarding tasks, and a second conversion refused; a referral filed by a plain employee, the same person referred again by a colleague writing **nothing** and telling them nothing, and settling refused to the referrer and to finance (audited `.denied`) and refused to HR as `recruit_referral_not_earned`; the retention job **401 without its bearer**, anonymising one candidate and leaving the consented one untouched, a no-op the second night, with the funnel still counting the anonymised application. Repeated greps of `audit_log`, `notification`, `email_outbox` and `application_event` for any figure or any scorecard text: **0 rows**. **Demo data verified from an empty database** (`drop schema public cascade`, **and the `drizzle` schema or migrations are silently skipped**, all 65 migrations, both seeds).
> **Not yet verified:** anything in a real browser or on a phone — no form, board or calendar has been touched by a hand, and the kanban drag was exercised only through its select-and-move twin; the English rendering; **the Google Calendar driver (written, never run)**; whether the careers page reads well on a phone, which is where most applicants will open it; and what the letter looks like printed.
> **Needs the owner:** **the careers page is public and indexable** — that is a decision, and `(public)/layout.tsx` is where to change it. **`RESEND_API_KEY`**: with no key every candidate email is written to the outbox and marked `skipped`, so **no candidate has ever actually been written to**. **There is no virus scanner**: every CV and take-home is stored `not_scanned` and reachable only by the hiring team, which is a mitigation and not a solution. **Google Calendar/Meet** needs a service account and the four env vars in `.env.example` before the untested driver can be tried. Confirm the **12-month retention window**, the **rate limits** (6 apply / 60 form per hour), the **consent wording** (`recruit.careers.consent.*` — bump `CONSENT_VERSION` when it changes), the **referral bonus rule** (earned once the hire is past a 2-month probation; the amount and the paying are payroll's), and the seeded **`TM-NHAN-VIEC` offer letter**, which like Phase 6's templates is **a draft nobody qualified has read**. Whether `hr_staff` should be able to see a salary band at all is the owner's call; today they cannot.
> **Deferred:** FR-REC-12 (AI assist — CV parsing, JD drafting) is a C item and untouched. No pipeline editor screen (`saveRecruitPipelineAction` exists and is authorized but is reachable only by posting) and no interview-kit editor. No cost per hire — nothing records recruitment spend, and the report says so rather than inventing a number. No offer email to the candidate (`sendCandidateEmail` with `OFFER_NOTE` is the intended path; the outbox is plain text and cannot attach the letter PDF, which is also why the `.ics` is linked from the interview page rather than attached). Payroll's `quoteOffer` (net → gross) is not wired into the offer form, and an offer's allowance total reaches payroll as a sentence in a note rather than as pay components. Nothing sweeps lapsed offers in the background (`effectiveOfferStatus` reads expiry from the clock, so the screens are right). No calendar month view, no interviewer free/busy beyond interviews and leave, no bulk email from the board, no filters or pagination on the careers page. ~~The same unqualified-column trap is still live in `kb/spaces.ts` (page counts) and `work/teams.ts` (member counts)~~ — **checked afterwards and false.** Drizzle qualifies interpolated columns as soon as the surrounding query has a join, and renders them bare only for a single-table select (confirmed by printing `.toSQL()` for both shapes). The trap therefore bites exactly where the retention job was written — one table, no join — and not in `kb/spaces.ts`, `kb/acknowledgements.ts`, `work/teams.ts` or `work/leader.ts`, which all join and all emit `"kb_space"."id"`. Their counts are correct and their tests were right. **The rule to remember: a correlated sub-select inside a single-table query is the dangerous shape** — losing a join from a query that has one would silently break it, so re-check any such sub-select when removing a join.

**Scope:** FR-REC-01..06, 08, 09, 13 · S items: FR-REC-07, 10, 11 · FR-CHR-17

| Week | Deliverables |
|---|---|
| 1 | Hiring requests + approval, job openings, configurable pipelines, candidate database with duplicate detection |
| 2 | Public careers page + application form (portfolio links, consent notice, spam/rate limiting), email templates, kanban pipeline |
| 3 | Interview scheduling with Google Calendar/Meet (incremental OAuth scopes), scorecards with blind feedback, assignments |
| 4 | Offer approval + offer letter, convert to pre-boarding employee, referral programme, funnel reports, candidate retention/purge job |

**Exit criteria**
- One real opening runs end to end: application → interviews → offer → employee record, with no retyping.

### Phase 8 — Performance reviews + year-end bonus (3 weeks) → **M8**

> **Status, 2026-09-20 — all three weeks built on local branch `mvp` (not pushed, not merged, not deployed); `pnpm check` (2,383 tests) and `pnpm build` pass; migrations 0066–0068, all additive (new tables, triggers, one exclusion constraint, one partial unique index). New RBAC permission: `performance:decide` (owner only — the result override and the weighting decision); policy tests added and `docs/permission-matrix.md` regenerated. Both exit criteria are demonstrated on 2026 demo data; neither can be *met* without the owner, because they are about the real 2027 cycle and the real bonus.**
>
> **Done — review cycles (`src/modules/performance/`, FR-PRF-03, 08):** form templates (`review_template`: competency sections, per-form question sets, and a rating scale whose points carry **what each is worth in basis points** — configuration, not a constant); cycles (`review_cycle`: probation / mid-year / annual, a timeline of self, manager, peer, calibration and release dates) that **snapshot the form onto the cycle at launch**, so editing a template afterwards cannot change what somebody was asked or what their answers were worth; participants materialised at launch with the **manager snapshotted**, so a reorganisation mid-cycle does not hand a half-written review to somebody new; self and manager forms with drafts private to their author until submitted; a manager review that cannot be submitted before the self review is in (or its due date has passed); the stage machine pending → self_done → manager_done → calibrated → released → acknowledged. **Confidentiality (FR-PRF-08) is a pure policy**: the subject reads their own review **only after release**, every manager above them always, holders of `performance:read` / `performance:manage` covering them — never a colleague.
> **Done — peer / 360 and release (week 2):** nominations (the subject nominates and the manager approves, or the manager / HR asks outright), the cycle's min and max peers enforced, and **anonymity honoured** — when the cycle is anonymous the subject never learns who wrote what, while HR and the management chain do. Release per participant and in bulk, with a notification; acknowledgement with an optional note.
> **Done — evidence panel (FR-PRF-07):** on the review form, each source read **through the owning module's `service.ts`** — OKR and KPI results (performance), task statistics (`work/service.getPersonTaskStats`, added), kudos (`comms/service.listKudos`), attendance (`attendance/service.summarisePersonYear`, added). Personal tier: shown to whoever may read the review, never wider.
> **Done — the final result per person per year (FR-PRF-09):** `performance_weighting`, an **effective-dated version the owner approves** (HR proposes, the owner decides, approved versions never overlap — the same governance as a pay policy); a **pure engine** (`engine/result.ts`, 10 hand-derived golden cases) combining the released review score, the stored KPI score and an OKR mix across the levels the person has goals at, where **a missing component hands its weight to the others** rather than scoring zero; `performance_result` rows carrying the computed figure, the owner's override **beside** it with its reason, the band, the multiplier, the whole trace, and the `kpiScoreIds` and `goalIds` it came from; compute → override → lock → publish, with an unlock that refuses once anything has been paid from the year.
> **Done — the year-end bonus (`src/modules/payroll/`, FR-PAY-21, SRS D13):** `bonus_scheme`, a versioned, effective-dated configuration with the same propose → owner-decides governance and a database exclusion constraint against overlap — pay component, base component, reference day, service-time bands, individual multiplier bands, unit-OKR level and bands, cap and rounding unit. **No part of the formula is a constant in code.** A **pure engine** (`engine/bonus.ts`, 13 golden cases with the arithmetic derived by hand in each fixture, plus monotonicity and cap properties): `amount = base × service × individual × unit OKR`, capped, rounded, integer VND, bigint throughout. `bonus_run` spans the **whole group**, is simulated as often as HR likes (and against a different multiplier table, storing nothing), then HR proposes → the **owner** adjusts individual amounts with a required reason → the **CEO** approves → it is paid as **one off-cycle payroll run per entity through `createOffCycleRun`**, which does the tax, the payslip and the bank file. Every step is a signature in an append-only `bonus_run_event`; a paid run and its lines are frozen by a database trigger.
> **Done — the reopen guard Phase 3.5 asked for:** approving a bonus run records every stored KPI score it was computed from in performance's own `kpi_score_use`, and `reopenMonth` then refuses those months (`kpi_month_consumed`, naming the run). A return or cancellation before payment releases them again. The freeze is **targeted**: other closed months of the same entity stay reopenable.
> **Done — S items:** 1:1 notes (FR-PRF-04) with a shared agenda, shared notes and **manager-private notes the subject never reads**, whose action items become real tasks in the task engine; review outcomes (FR-PRF-06) raised from a settled result — a promotion, development plan or PIP raises an HR task, and a **salary adjustment goes out through payroll's own `submitSalaryChange`**, encrypted and approved there, so performance never becomes a second way to set a salary.
>
> **Decisions taken (nobody to ask):**
> · **The bonus lives in payroll, the reviews and results in performance.** An amount is compensation tier, and `payroll/policy.ts` already refuses a line manager, a department head and an entity director every figure; performance publishes the *multiplier* and payroll turns it into money. The dependency therefore runs **payroll → performance/service only**, and there is no import cycle.
> · **The KPI-reopen guard sits in performance** (`kpi_score_use`), written by payroll through `markScoresConsumed` when a run is approved. That is what lets `reopenMonth` refuse without performance ever importing payroll.
> · **The first bonus formula (SRS open question Q14), for the owner to change:** one month's salary at "meets expectations" with a full year of service; service factor 0 / 0,25 / 0,5 / 1,0 at 0 / 3 / 6 / 12 months; individual multiplier 0 / 0,5 / 1,0 / 1,25 / 1,5 across the five result bands; the entity's OKR year moving everything by 0,8 / 1,0 / 1,2; capped at three months; rounded to 1.000 đ; collaborators excluded, three months' service and a settled result required. The result weighting is KPI 50 %, review 30 %, OKR 20 %, with the OKR share 50 % individual / 20 % department / 20 % entity / 10 % group.
> · The scheme offers two sources for the individual multiplier: the band FR-PRF-09 published (the default) or the scheme's **own** table — which is what the "what would a different multiplier table cost?" simulation uses.
> · Somebody ineligible **stays in the run** with an amount of 0 and the reason on their line, because "why did I get nothing?" is the question the run has to answer. The owner may still grant them an amount; that is an override with a reason like any other.
> · No column holds a bonus amount in the clear: each line's whole trace is envelope-encrypted and bound to its row, like every other figure in payroll. The clear columns are ids, bands, multipliers, service months, flags and the words of a reason.
>
> **Verified locally** over HTTP with forged sessions as the owner, the CEO, the HR lead (C&B), entity HR staff, another entity's C&B, the chief accountant, a department head who is also a line manager, and plain employees. **The tier line, live:** the line manager opens `/performance/results` and reads his report's review 112 %, KPI 99,47 %, OKR 65,49 %, final 96,43 % and band "Đạt" — and gets the same 404 as a stranger on that person's bonus page. The person reads their own. `/payroll/bonus` and the run page open for the owner, the CEO, the HR lead and finance; another entity's C&B, HR staff, the department head and employees get 404. Actions: creating a run refused for HR staff and for C&B whose reach does not cover every entity; **the individual override refused for the CEO and for C&B — the owner alone**; HR proposes but cannot approve; the CEO's return refused without a comment; every refusal in the audit log as `.denied`, and every audit row counts and ids only, never a figure. **The reopen refusal proven live**: group HR's reopen of a consumed month came back `kpi_month_consumed` naming the bonus run. **Two real bugs found this way, both now with a failing-without-the-fix test:** `payBonusRun` stamped the payroll run onto every line of the entity, so an excluded person's page claimed they had been paid; and the 1:1 create action asked "am I the manager named on this row?", which is always true of whoever is posting — any colleague could open a 1:1 record about anybody.
> **Demo data:** `pnpm db:seed && pnpm db:seed:demo` leaves the completed 2026 SuZu Media cycle (13 forms with self, manager and peer input, 6 nominations, 4 released and acknowledged, 4 published results, one with an owner override) and the approved group-wide bonus scheme. Then `pnpm dev` and **`pnpm db:seed:demo:bonus`** runs the whole chain through the real use-cases: 8 people, 4 paid (99.400.000 đ, of which 5.000.000 đ is an owner adjustment with its reason), 4 excluded for four different reasons, 11 KPI scores frozen, and one calculated off-cycle payroll run for January 2027. `/payroll/bonus/<run>/<person>` then explains any amount by hand: 20.000.000 × 1,0000 (41 months) × 1,0000 (Đạt) × 0,8000 (the company below target) = 16.000.000 đ.
> **Not yet verified:** anything in a real browser (the forms, the JSON scheme editor, the trace page on a phone); the English rendering; a cycle with several entities at once; the peer-anonymity rendering with a large peer set; performance of a run over hundreds of people (the build loop is one person at a time).
> **Needs the owner:** approve a real bonus scheme version and a real weighting version (the seeded ones are the placeholders above, and SRS Q14 is answered by them only provisionally); decide whether the individual multiplier should come from the published band or a scheme table of its own; confirm the eligibility rules (three months, collaborators out, employment on 31 December); run the real 2027 annual cycle and the real bonus (both exit criteria); confirm that the CEO reading the run register but not one person's amount is the right split.
> **Deferred:** a structured band-table editor for the scheme (it is edited as JSON today, validated in full by the same schema the engine reads); review outcomes are not part of the seeded demo (they were exercised over HTTP); calibration sessions as a screen of their own (the stage exists and the note is stored); training completion in the evidence panel (no training module exists); reminders for an unwritten review or an unacknowledged one; a bonus report or export; paying a bonus in instalments; re-running a bonus year after a correction (the guard refuses the reopen on purpose — a correction belongs to the next year). To re-seed the demo bonus run locally the paid-run triggers must be disabled around the delete; that is deliberate.

**Scope:** FR-PRF-03, 08, 09 · FR-PAY-21 · S items: FR-PRF-04, 06, 07 (goals and KPIs already shipped in Phase 3.5)

| Week | Deliverables |
|---|---|
| 1 | Review cycle builder: form templates, participants, timeline, self + manager review, confidentiality rules |
| 2 | Peer/360, release and acknowledgement, evidence panel (goals, KPI scores, task stats, kudos, attendance); **final performance result per person per year** with configurable weighting, owner override with reason, lock and publish |
| 3 | **Year-end bonus scheme**: formula and multiplier bands, cost simulation, HR proposal → owner adjustments → CEO approval → off-cycle payroll run; 1:1 notes with action items → tasks; outcomes → lifecycle proposals |

**Exit criteria**
- The 2027 annual review runs in-system.
- The 2027 year-end bonus is simulated, approved and paid through an off-cycle payroll run, with each person's amount traceable to their KPI/OKR results and any owner override.

This phase must be live by **November 2027** to serve the year-end review and the bonus paid before Tết 2028.

### Phase 9 — AI assistant + analytics (3 weeks) → **M9**

**Scope:** FR-AI-01, 02, 06 · FR-RPT-01 (v2), 04, 05 · C items as time allows

| Week | Deliverables |
|---|---|
| 1 | Retrieval over the KB with permission filtering and citations; chat UI; vi/en; unanswered-question log; evaluation set of ~100 real HR questions |
| 2 | Permission-checked personal tools (leave balance, payslip explanation, approver lookup, attendance summary); guardrail tests (no cross-user leakage); audit logging of tool calls |
| 3 | Owner dashboard v2, scheduled reports, work analytics; optional drafting helpers and confirm-to-act shortcuts |

**Exit criteria**
- At least 85% of the evaluation set is answered correctly with a correct citation.
- A red-team test shows no cross-user or compensation leakage.

> **Status, 2026-09-20 — all three weeks built on local branch `mvp` (not pushed, not merged, not deployed); `pnpm check` (2,498 tests) and `pnpm build` pass; migrations 0069–0071, all additive. Both exit criteria are met, with the honest caveat below. One new module, `src/modules/ai/`, and one new module, `src/modules/reports/`. No new RBAC permission.**
>
> **Done — Ask SuZu, the retrieval half (`src/modules/ai/`, FR-AI-01):** a chat at `/assistant` in Vietnamese and English that answers from the knowledge base **with citations**, built on Phase 4's `retrieveKbChunks(viewer, …)` — **the permission filter is the same SQL the KB's own pages use**, so a passage the asker could not open is never loaded, let alone quoted. Ranking on top of it: the vector score plus a lexical score (corpus IDF over the candidates, peak term, word pairs) plus a heading bonus. **Citations are chosen from the retrieval result, never parsed out of model text**, so they stay correct when a real model is plugged in. Conversations and turns are stored (`ai_conversation`, `ai_message`); a question nothing answers goes to the **unanswered log** (`ai_unanswered_question`) with the asker, the language and the best score, listed and closeable at `/assistant/unanswered` under `kb:manage`. Every question is a `createAction` mutation, so `ai.ask` is audited with the question, the outcome, the score and the cited page ids — never the answer body.
>
> **Done — the model adapter (`ai/model.ts`), and why the numbers below mean what they mean:** there is **no LLM key**, so the default driver is **local and extractive**: it generates nothing at all — the answer *is* the best passage of the page, with its citation. A **Claude API driver is written and has never been run** (`claude-opus-5`, adaptive thinking, `ANTHROPIC_API_KEY` / `ANTHROPIC_MODEL` in `env()` and `.env.example`); it is unverified in exactly the way the Voyage embeddings driver and the Google Calendar driver are. Embeddings are still Phase 4's deterministic local fake (`fake-hash-256`), which is a bag-of-words cosine.
>
> **Done — the personal tools (FR-AI-02, `ai/tools.ts`):** four tools — **leave balance**, **attendance summary**, **approver lookup**, **payslip explanation** — each reading through the **owning module's `service.ts` with the asker's own principal**, never elevated: `getLeaveBalanceFor` (new viewer-scoped door in `leave/ledger.ts`), `getMonthSummaryFor` (new in `attendance/timesheets.ts`), `previewApprovers` (new in the approval engine), `listMyPayslips` + `getPayslipView`. Four invariants hold the line: **the subject is always clamped to `user.person.id`** — a name in a question is never resolved to a person; the owning module re-decides; the payslip tool demands the same **fresh step-up** the payslip page does; and a tool answers in **message keys and numbers**, never prose, so the function that builds a prompt *throws* rather than put another person's figures into one. **Routing is a pure function of the question and runs before retrieval**, so no page, no model and no key can reach a tool. Each call is audited as `ai.tool.<name>` with the outcome and no figure.
>
> **Done — prompt injection treated as the default case:** retrieved text never enters the system prompt; it sits in one escaped `<source>` block framed as reference material, with the guard repeated after the data. A hostile KB page is seeded and tested: it does not redirect the answer, it does not cause a tool call, and it cannot move the subject off the asker.
>
> **Done — owner dashboard v2 (FR-RPT-01, `src/modules/reports/`):** `/reports`, eight tiles — headcount and movement, payroll cost trend per entity, attendance today, leave today, open positions, overdue obligations, tasks at risk, approvals waiting on me. Each tile is **one call into the owning module's `service.ts` with the reader's own principal**; the reports module owns no query of its own. A tile the reader may not see is **absent, not zeroed**; a tile that throws is dropped and logged rather than breaking the page.
>
> **Done — scheduled reports (FR-RPT-05):** `report_schedule` / `_recipient` / `_run` and a `report-schedules` job on the morning schedule. The report is built **once per recipient, as that recipient** (their own grants), so somebody who may no longer read it is recorded `not_permitted` and sent nothing. Cadence arithmetic is a pure engine (a missed day is caught up once; day 31 clamps per month). Delivery is the existing email adapter: **with no `RESEND_API_KEY` every scheduled report is written to the outbox and marked `skipped`**, and the screen says so.
>
> **Done — work analytics (FR-RPT-04):** `/work/analytics` — throughput, on-time rate, workload and revision rounds by team and client, a pure engine with tests behind a query filtered in SQL by the same `visibleTaskCondition` the work pages use. Undated completions are counted apart and left out of the on-time rate.
>
> **Exit criterion 1 — the evaluation set: 93.1% (149/160).** 160 questions written from the seeded Vietnamese handbook and the SRS, run as four real personas through the same `resolveAnswer` that answers people; `pnpm ai:eval` re-runs it against a local dev server. By kind: tools 16/16, knowledge-base answers 126/134, red team 6/6, "should refuse" 1/4. By language: vi 138/146, en 11/14. The right page is cited somewhere in the answer for 98.5% of answerable questions. **What this does and does not prove:** it proves that **retrieval, permission filtering and citation are right** — on the local driver the answer *is* the passage, so nothing is being measured about generated prose, faithfulness or tone. It says nothing about how the Claude driver will behave, and the questions were written from the seeded content by the same session that built the retriever. The 3-of-4 failure on "should refuse" is honest and unfixed: the threshold that would refuse those three also refuses correct answers (weakest correct 0.362, strongest wrong 0.379), and the threshold was deliberately **not** tuned against the set.
>
> **Exit criterion 2 — the red team: no leakage.** `src/modules/ai/guardrails.test.ts`, 19 cases against a real payroll run, leave ledger, timesheets and knowledge base. Proved closed: a colleague's leave balance; **a line manager asking for their own report's pay and their own report's lateness**; HR staff asking for a payslip they hold no payroll permission for; **the owner asking for another person's payslip through the assistant**; compensation on a session that has not re-authenticated; an injected page naming a tool and a person. Every refusal is paired with the same question about the asker's own data, so none of them is vacuous, and a refusal carries no figure and never enters the unanswered log.
>
> **Decisions:** the assistant gets **no RBAC permission of its own** — everybody may ask, and what it can answer is decided per asker by the access rows already in the database; somebody who can open no page gets "I do not know". Likewise scheduling: **you may schedule what you may already read**, and being named a recipient grants nothing. **The payroll cost report may never be emailed** (`schedulable: false`): compensation is read after proving who you are, and a mailbox proves nothing. The unanswered log sits under `kb:manage`, because it is a list of gaps in the handbook and the handbook's keepers are the ones who close them. A tool's subject is the asker, full stop — no manager-scope tools in this phase.
>
> **Verified locally:** the eval and the guardrail suite above; over HTTP with forged sessions as four personas — the same question answered from a restricted page for the department head and refused for the plain employee; a tool answer, a cross-person refusal and an own-payslip answer; `/assistant/unanswered` 404 for an employee and 200 for HR; the dashboard showing the owner headcount and payroll cost, the department head a scoped headcount and **no payroll tile at all**, a plain employee three tiles; `/reports/schedules` 404 / 200; a manual cron run delivering one schedule to three recipients and recording `not_permitted` with zero emails for the fourth; `ai.ask`, `ai.tool.*`, `report.export.denied` and `report.schedule.*.denied` rows in the audit log. Three real bugs were found this way and not by tests: a service call inside its own transaction deadlocked; a `server-only` value imported into a client component put postgres in the browser bundle; and **`exportReportAction` checked `canSee` but not step-up, so the payroll cost CSV was reachable on a two-hour-old session while `/payroll/reports` redirects** — fixed with a per-report `stepUp` flag. Also found: `[A-ZÀ-Ỹ]` does not match lowercase Vietnamese, so "đơn nghỉ phép" was read as a person's name and refused every leave question.
>
> **Not yet verified:** **the Claude driver — never run, no key.** The whole phase's behaviour with a real model: faithfulness, tone, refusal quality, whether it obeys the injected-page guard, and tool use through the API's own tool protocol (the executor is shared with the local driver, so the permission checks cannot be bypassed by switching, but the API-side call shape is untested). The Voyage embeddings driver is still unrun, so **the retriever is lexical**, which is why English questions about Vietnamese pages are the weakest bucket — a bilingual HR glossary was added to close part of that gap, and a multilingual embedding model should close the rest. No scheduled report has ever reached a mailbox. Nothing in a real browser.
>
> **Needs the owner:** `ANTHROPIC_API_KEY` (+ `ANTHROPIC_MODEL`) before the assistant generates anything, and the zero-data-retention setting on that account (FR-AI-06 asks for it and the driver cannot assert it by itself); `EMBEDDINGS_API_KEY` and a re-embed before the retriever is more than lexical — **re-run `pnpm ai:eval` after both and compare, because 93.1% is a local-driver number**; `RESEND_API_KEY` before any scheduled report reaches a mailbox; the real handbook (Phase 4's exit criterion) — the evaluation set is written against the demo pages, so it must be rewritten against the real ones before it means anything about the real company; confirm the decisions above (no scheduling permission, payroll cost never emailed, the unanswered log under `kb:manage`, tools about the asker only); decide whether a recipient who is not the creator should see the schedule that sends to them (today they cannot); Phase 3's open question about the owner and private projects now also shapes the work report.
>
> **Deferred:** **FR-AI-03 (confirm-to-act shortcuts) and FR-AI-04 (drafting helpers) were not built** — both are C items and neither was nearly free: each needs its own routing, actions and guardrail pairs. FR-AI-05 (natural-language questions over reports) likewise. Manager-scope tools (resolving a name to a person, then re-checking `readableTier`). Streaming answers. Feedback on an answer ("this was wrong"). pgvector and an HNSW index — `retrieveKbChunks` still ranks in the application over at most 2,000 permission-filtered candidates, which is right for a handbook and wrong for a large corpus, and it is still the single seam to change. A schedule editor for report parameters beyond the catalogue's defaults. `.xlsx` scheduled reports (CSV today).

### Phase 10 — Projects & daily work management (PJM) (12 weeks, six releases) → **M10**

> **Status, 2026-09-23 — all six releases built on branch `phase-10-pjm` (not pushed, not merged, not deployed). Migrations 0078–0087, all additive. `pnpm check` and `pnpm build` pass (figures in the last commit message).** Every FR-PJM item is built except the ones listed under *Not built*. None of the people-dependent exit criteria can be met without the owner (two pilot teams for a month).
> **Done — the project layer (`src/modules/projects/`):** project types and job numbers (`<ENTITY>-<YY>-<NNN>`, row-locked counter; plan rows made by the midnight `project-plans` job and on the first change); brief with a **kick-off gate** on the approval engine (request type `project_brief`; approval takes the baseline and makes the project active); phases, milestones (client-facing / billing), the **deliverables register** (units = linked tasks, status from a pure engine fed by client decisions, deliveries and the publish log); hours budget by role with burn = logged + remaining estimates and alerts at 80/100 %; fee only with `pjm:commercial`; **status updates** with required health and facts prefilled (incl. open high risks and issues), stale flags, reminders; **portfolio** with filters and CSV; project templates v2 (plan half in `project_template_plan`); **timeline/Gantt** (SVG, virtualised rows, drag/resize, dependents shifted by working days after asking, baseline ghost bars, critical path, keyboard, read-only on phones); re-baseline (audited); **bookings** with placeholders, tentative vs confirmed, and a capacity page (filter by position — there is no skills inventory); **retainers** (monthly periods with rollover, quotas, overservicing, alerts, hours allowance, monthly fee billed at month end); **change requests** (delta on lines, hours, fee and date; a fee change adds a `pjm:commercial` step); **acceptance** (biên bản nghiệm thu from a document template, signed scan) → **billing queue** for finance (idempotent items per acceptance, milestone and retainer month); client report PDF; **close-out** with a live checklist, override with a reason, close report and retrospective; RAID-lite with decisions and issue → task; meetings with decisions and action items → tasks; **project document spaces** on the KB engine (access row `project:<id>`; `kb:manage` does not reach them).
> **Done — the day (`src/modules/daily/`):** `/today` is the landing page (planned, due, reviews, hand-offs, blockers, bookings, timer, quick add, days off); morning plan; **end-of-day report prefilled from activity** (target ≤ 60 s, `seconds_to_submit` recorded), team board for leads and line managers with one-click reminders, comments; weekly reports (person and team, Mondays); per-team rules (plan/report modes and days, cut-off and deadline, time mode, timesheet approval, cover threshold, cycles; strictest rule wins); **timer and time entries**, week grid, **weekly timesheet approval** (lead or line manager, never oneself; approved = locked; reopen with a reason); time vs attendance hint; utilisation (no individual figures for portfolio-only readers).
> **Done — the task foundation (`src/modules/work/`):** custom fields (9 types) in list, table, board and filters; **table view** with inline edit and bulk edit; moving a task between teams with the old number kept; **triage inbox** with rules (intake forms and cross-team hand-offs land there); **blockers**; cycles with rollover; **stage hand-off packages** enforced on every state change (one service path), accept/return with return counts and waiting time, cross-team hand-offs through triage, one note format; **leave cover plans** pulled from leave requests; **exit/transfer handover** gating the offboarding checklist step (a generic completion-guard registry in the task engine); account handover; **review chains** with stages up to the client; **client decision records** with required evidence and frozen versions; image and video pins, version compare; delivery records; **publish log** (a Published state needs the URL) and results with CSV import; **automations** ("when … then …", in the same transaction, one level deep, savepoint per rule, presets). The app-frame badge now counts review stages, pending hand-offs and blockers (0085).
> **Done — insight:** `/reports/delivery` (health, stale updates, milestone slip, on-time, register, hours burn, retainer consumption, revision rounds internal vs client, returned hand-offs, blocked time, EOD and timesheet compliance; schedulable); **KPI actuals proposed from work** (`kpi_definition.work_metric`; a proposal is never scored until the scorer confirms it); **profitability** (`pjm:cost` + `pjm:commercial`, step-up, audited; loaded cost rates from signed payroll runs, never per person); AI drafts for EOD notes, status updates and hand-off notes (never saved automatically; the local driver drafts extractively).
> **Decisions made during the build** (confirm or overrule): people in no work team get optional plan/report; leads are reminded too; half a day of leave still needs a report; a team's working weekdays = its report days; no schedule = 8 h Mon–Fri; the kick-off approval sets the project active, and projects created without a template stay `active` with a notice; an approved brief is locked (changes go through change requests); only leads decide triage; a week with a running timer cannot be submitted; time can be edited 35 days back; the manager's manager reads a timesheet but does not decide it; over-delivery rolls over as a negative quota for one month; part months get a day share of quota, hours and fee; a client-facing milestone that is not a billing milestone makes an amount-less billing item; "Published" is recognised by the state's name; the publish check applies to content tasks only; client "approved with changes" freezes the version; profitability needs both `pjm:cost` and `pjm:commercial`; a KPI's scorer is the person's manager, else HR; an entity director (holding `pjm:commercial`) may mark billing items invoiced.
> **Verified locally** on the Docker database with a PJM demo seed (`pnpm db:seed:demo:pjm`) and forged sessions, twice: once after the build and once after the fixes. The second pass swept 1,738 pages across 17 personas with the scripts stripped, hunting a private project's name, job number, task titles, blocker text and every fee — **no leak, no 5xx, no untranslated key**; it also proved the retainer month bounds, that a whole-project acceptance and its milestones cannot both bill the fee, that closing a project stops its retainer, the cover and exit-handover rules, the timesheet window and locks, team-membership scope, automations refusing to act for an author who may not manage a private project, calendar invitations reporting honestly that nothing was sent, that reading a project writes nothing (2,356 page loads, no new rows), and every cron slot twice with nothing new the second time. Screenshots on desktop and phone.
> **What the reviews caught:** a security review found 25 issues (private-project and fee leaks through approver views, automations, account and exit handover, daily report titles; double billing paths; locking races) and the final pass found a 26th — the assignable list, cover candidates, hand-off receivers and the exit handover all treated a private project as the whole team, and the task update never checked the assignee at all. All are fixed with regression tests; `pnpm check` runs 3,086 tests.
> **Built after the owner's answers (2026-09-23, D22–D30):** time logging required of every team with weekly approval; the plan and the end-of-day report required of everyone on every scheduled working day, due 23:00, readable up the whole management chain; **client review links** at `/preview/<token>` — the app's second public surface (FR-PJM-51a): one version pinned at creation and only ever one that has cleared internal review, a token kept as a hash, revocable, rate-limited, `noindex`, its own small vocabulary rather than the app's message catalogue, a visitor fingerprint that re-keys daily, and one decision that writes the same client decision record and freezes an approved version; a project's lead and account manager read their own project's money while `pjm:commercial` still writes it; **no client work is billed without a signed acceptance**, milestones and retainer months alike; the owner and `pjm:portfolio` holders read a private project — recorded in the audit log wherever the read is resolved — without being able to touch it. Two further security reviews and two verification passes on the local database covered these; every finding was fixed with a regression test.
> **Not built:** per-project weekly reports (the project status updates cover them); a skills inventory for capacity planning, which filters by position instead (FR-CHR-14 has no records yet); platform APIs for post results, which are entered or imported (L). Google Calendar for meetings and the RAID-owner notice were built after this note was first written; the calendar adapter now lives in `src/modules/platform/calendar/` and serves both interviews and meetings.
> **Needs the owner:** Q17–Q25 are answered (D22–D30) and built; the owner accepted the acceptance template's wording and gave finance the portfolio right on 2026-09-23. What is left before the **5 October 2026** cut-over (D28):
> 1. **Merge and deploy** this branch — nothing of Phase 10 is on `main`, and the whole HRM go-live list in `docs/MVP_STATUS.md` §4 still stands.
> 2. **`pnpm db:seed` on production**: the project template plans and the acceptance template `TM-NGHIEM-THU`. Counsel reading that template once is still worth the hour — it is the door every client invoice passes through.
> 3. **Fill in the real work**: teams, clients and brands, retainers with their monthly scope, the open projects with their briefs and registers, and each project's lead and account manager. The fortnight before the cut-over is for this.
> 4. **Announce the cut-over**: which Google Sheets stop being the record on 5 October, and that from that morning time, plans and end-of-day reports are expected of everyone (D22, D23).
> 5. **Two settings**: the new **evening** cron (`vercel.json`, 18:00 ICT) sends the end-of-day reminders; video deliverables above 50 MB need Supabase's project-wide upload limit raised to 200 MB (a paid-plan setting).
> 6. **Review the rules the build chose** above — they are listed so they can be overruled.

**Why now.** HRM is usable (phases 0–9 are on `main`). The owner's next goal (SRS D21) is that employees do *all* their daily work in the app — planning, executing, hand-off and delivery. Phase 3 built the task foundation (teams, workflows, projects, tasks, reviews, templates, My work, leader view, workload, intake); PJM adds the project layer above it, the daily loop around it, and the hand-off and delivery records after it. No second task model (ADR-10): everything hangs on `task`, `work_project` and `work_team`.

**Scope:** SRS §4.6b, FR-PJM-01..64, and the §4.6 upgrades it lists (FR-WRK-04, 05, 12 raised to M).

**Architecture notes**

- New module `src/modules/projects/` for the project layer (brief, phases, milestones, register, retainers, change requests, status updates, RAID, meetings, acceptance, billing, close-out) and new module `src/modules/daily/` for the person's day (plan, EOD report, time entries, timesheet weeks). Hand-offs, triage, custom fields, automations and the review chain extend `src/modules/work/`. Each keeps the usual `schema.ts` / `service.ts` / `policy.ts` / `actions.ts` / `engine/` / `ui/` shape; cross-module reads go through `service.ts`.
- **Pure engines with golden tests:** EOD prefill (activity → report draft), budget burn and alerts, retainer period generation and quota consumption with rollover, overservicing, timeline scheduling (dependency shift, working days), capacity (schedule − leave − holidays − bookings), utilisation, job-number formatting, hand-off package completeness, automation rule matching, project health facts, close-out report.
- **Pulled, not pushed** (Phase 3 decision kept): leave cover reads approved/pending `leave_request` rows; exit handover reads `lifecycle_event` rows; KPI actuals are proposed by a job that reads PJM tables. HR modules never import PJM.
- **Money split:** hours are ordinary work data; fees need `pjm:commercial`; cost rates are compensation tier, computed by a payroll-owned function that returns only aggregates, read only with `pjm:cost`.
- Automations run in the same transaction as the triggering change, bounded (no rule may trigger rules more than one level deep), and are written to `work_activity`.

| Release | Weeks | Content | SRS |
|---|---|---|---|
| **R1 — Plan** | 2 | Project types, job numbers, project roles (lead, account manager, member, viewer), brief + kick-off gate on the approval engine, phases and milestones, deliverables register, hours budget with burn, **project status updates with health**, portfolio view, project templates v2, custom fields, table view with bulk edit, moving tasks between teams; permissions `pjm:commercial`, `pjm:cost`, `pjm:portfolio` | FR-PJM-01..05, 08, 09 (hours), 14, 15, 27, 34..36 |
| **R2 — Daily loop** | 2 | **Today** page as the landing page, morning plan, **EOD report prefilled from activity** with the team daily board and one-click remind, weekly report, blockers, **triage inbox** per team (intake forms and cross-team requests land there), mobile quick actions | FR-PJM-20..23, 28, 32, 37 |
| **R3 — Time & capacity** | 2 | Time entries (timer + manual, billable), weekly timesheet submit → approve → lock, time vs attendance hint, bookings with placeholders (tentative / confirmed) on the workload data, utilisation, cycles, baselines, **timeline / Gantt**, fee budgets | FR-PJM-07, 09 (fee), 10, 12, 13, 24..26, 61 |
| **R4 — Hand-off** | 2 | Stage hand-off packages on workflow transitions, accept / return, cross-team hand-off through triage, one hand-off note format, **leave cover** from leave requests, **exit / transfer handover** gate in the offboarding checklist, account handover | FR-PJM-40..46 |
| **R5 — Delivery** | 2 | Review chains, **client decision records** with evidence and frozen approved versions, image pin comments and version compare, delivery records, **publish log**, **retainers** with monthly quota and overservicing, change requests, **acceptance (nghiệm thu)** generated from the register, billing hand-off queue for finance, client report PDF, close-out with retrospective | FR-PJM-06, 11, 50..59 |
| **R6 — Insight & automation** | 2 | Automations (when … then …), RAID-lite and decision log, meeting notes with action items, project document spaces on the KB engine, delivery dashboards, KPI actuals proposed from work, profitability (owner / finance), AI drafting of EOD reports, status updates and hand-off notes | FR-PJM-29..31, 33, 60, 62..64 |

**Rollout (owner, 2026-09-23).** **Every team is a pilot team** (D29), so PJM is not staged behind the feature flag: the whole company starts together. The **cut-over is Monday 5 October 2026** (D28) — from the announced date the Google Sheets the teams run their work in are no longer the record, and work that is not in the app does not count. Time logging, the morning plan and the end-of-day report are required of everyone from that date (D22, D23).

**Exit criteria**
- Every team runs a full month in the app: brief → plan → daily reports → hand-offs → client decisions → publish log → signed acceptance → billing item, with no parallel Google Sheet.
- ≥ 90 % of people submit the EOD report on required days for two consecutive weeks, median submit time ≤ 60 s.
- Every client's work of that month carries a signed acceptance before it is billed (D27).
- Every open task of a leaver or a person on ≥ 2 days' leave is covered or reassigned before the leave starts or the checklist closes (verified on real cases).
- Finance receives every billing item for the pilot month from the queue, not from chat.
- No fee is visible to a person without `pjm:commercial` and no cost rate to a person without `pjm:cost` — proven by PGlite tests on every list, export and rollup.

### Phase 11 — CRM

A separate SRS will be written after Phase 10. It will reuse org, RBAC, the approval and task engines, clients/brands from the Work module, files, notifications and AI. The target outcome is deal → project → tasks → time → cost → **client profitability**.

---

## 4. Quality plan

| Area | Approach |
|---|---|
| **Engines** (payroll, timesheet, leave accrual) | Pure libraries; golden-file tests from real anonymised cases; property-based tests; every statutory change adds a dated test case |
| **Permissions** | A matrix test generates every role × scope × resource × action combination and asserts allow/deny; it runs in CI; new resources must register in the matrix |
| **Integration** | Service-level tests against real Postgres with transaction rollback |
| **End-to-end** | Playwright for critical journeys: sign-in (both domains + rejected Gmail), check-in, leave request → approve, timesheet lock, payroll run → payslip, task create → review → done, obligation complete with evidence |
| **UAT** | Each phase ends with a scripted UAT with named business owners (HR lead, chief accountant, two team leaders) on staging with anonymised data |
| **Security** | Dependency + secret scanning in CI; security review at M1 (auth/RBAC), M5 (payroll), M7 (public surface), M9 (AI) |
| **Performance** | Load test of the check-in peak and the payroll job before M2 and M5 |
| **Definition of done** | Requirement implemented · tests pass · vi + en strings · audit events emitted · permission matrix updated · mobile checked · docs/KB help page written · feature flag plan |

---

## 5. Rollout & change management

1. **Champions**: one HR champion, one finance champion, and one leader per creative team. They join UAT and train their teams.
2. **Pilot then expand** per module (department → entity → group) via feature flags.
3. **Help content lives in the KB** (from Phase 4; before that, short Google Docs). Each release gets a two-minute screen recording.
4. **Feedback channel**: an in-app "Report a problem / suggest" button creates a task in the product backlog project (available from Phase 3).
5. **Cut-over rules**: each milestone has an explicit date after which the old method (spreadsheet, chat approval, paper form) is no longer accepted — announced by the owner.
6. **Payroll** is the one module with a mandatory parallel run and written sign-off.

---

## 6. Risks

| # | Risk | Impact | Mitigation |
|---|---|---|---|
| R1 | Payroll calculation errors | Wrong pay, tax exposure, loss of trust | Pure engine + golden tests + 2-cycle parallel run + accountant sign-off; explanation trace on every payslip line |
| R2 | Legal parameters change (this happened twice in 2026: PIT reform, reference-level increase) | Incorrect statutory amounts | Effective-dated parameter store; "annual legal review" and "on-change" obligations in the ops tracker itself; nothing hard-coded |
| R3 | Personal-data compliance (PDPL, cross-border hosting). The owner chose Singapore hosting and deferred the legal work (SRS D10) | Legal exposure | Tracked as open item Q16; close it before payroll data goes live; portable architecture; consent/notice flows; retention jobs |
| R4 | Scope is very large for an owner + Claude Code team | Delays, half-finished modules | Strict phase exit criteria; M-priority first; S/C items deferred; re-baseline after Phase 1; the owner's build time is the critical resource — protect it |
| R5 | Low adoption of task management (teams stay on chat/Trello) | Wasted build | Build with two pilot teams; speed and mobile UX are requirements, not extras; owner-announced cut-over; templates that match real workflows |
| R12 | PJM report fatigue and time-tracking resentment (people fill reports on Friday, or pad estimates) | Fiction instead of data; lost trust | Reports prefilled from activity, manual part limited to blockers and plan; time logging only where work is billed in hours (SRS A10); never tied to pay; no company-wide individual rankings |
| R13 | Client approvals recorded without client accounts can be disputed | Rework at the client's expense argued as ours | Evidence required on every client decision, approved version frozen, acceptance signed as biên bản nghiệm thu (FR-PJM-51, 55); the client's own decision on an expiring review link (D24, FR-PJM-51a) is the stronger record where the client uses it |
| R14 | The client review link is a public, unauthenticated surface (D24) | A leaked link shows one deliverable to whoever holds it | One version per link and nothing else of the company, token stored only as a hash, short expiry, revocable, one decision, rate-limited, `noindex`, every view and decision audited |
| R6 | Biometric device integration varies by model | Attendance gaps | File import first (works with any device); direct integration only after models are confirmed |
| R7 | Two-workspace Google setup friction (consent screens, calendar scopes) | Login/integration issues | External OAuth client, both admins trust the app, incremental scopes, tested with accounts from both domains in CI |
| R8 | The owner is the only developer | Bus factor; the owner's time competes with running the company | Documented ADRs, conventional structure, high test coverage, runbooks, infrastructure as configuration |
| R9 | Sensitive-data leak via exports, search or AI | Severe | Tier-aware authorization at the service layer, used by all three; audit of reads; red-team tests at M5 and M9 |
| R11 | Employees on the Simple pay profile (cash, no insurance/PIT) who legally fall under compulsory insurance or PIT withholding | Back-payments, penalties and interest if inspected; disputes when a person claims insurance benefits | Basis recorded per person and owner-only exposure report (FR-PAY-08); review with the chief accountant; nothing is hidden from reports |
| R10 | Data migration quality | Bad starting data breaks trust | Import with validation preview; HR verifies per entity; ESS change requests let employees fix their own data |

---

## 7. Immediate next steps

| # | Action | Who |
|---|---|---|
| 1 | Build Phase 10 release R1 (Plan) on branch `phase-10-pjm` | Owner + Claude Code — **in progress** |
| 2 | Name the two PJM pilot teams and their leads (SRS Q24) | Owner |
| 3 | Decide the job-number scheme per entity, who sees fees, and whether private projects stay closed to the owner (Q20, Q21, Q25) | Owner |
| 4 | List the trackers and spreadsheets PJM will replace and pick a cut-over date (Q23) | Owner |
| 5 | Decide which teams log time and whether EOD reports are required for everyone (Q17, Q18) | Owner + team leads |
| 6 | Collect one real brief, one retainer scope and one signed biên bản nghiệm thu per pilot client as template sources (Q22) | Account managers |
| 7 | Everything in `docs/MVP_STATUS.md` §4 still stands for the HRM go-live | Owner |
