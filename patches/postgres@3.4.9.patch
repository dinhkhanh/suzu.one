diff --git a/cf/src/connection.js b/cf/src/connection.js
index 8e79170aeb13efe8c3c77ccede0cd3a115b5e1b5..8845ce91e9e0e650079c4de11752d564a2747b61 100644
--- a/cf/src/connection.js
+++ b/cf/src/connection.js
@@ -175,8 +175,10 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       return write(toBuffer(q))
         && !q.describeFirst
         && !q.cursorFn
-        && sent.length < max_pipeline
+        // suzu.one patch: onexecute must run whenever the query was written, even with pipelining
+        // off (max_pipeline: 0); begin() and reserve() rely on it to claim the connection.
         && (!q.options.onexecute || q.options.onexecute(connection))
+        && sent.length < max_pipeline
     } catch (error) {
       sent.length === 0 && write(Sync)
       errored(error)
diff --git a/cjs/src/connection.js b/cjs/src/connection.js
index 07f6716702ac888215c86ad6e0071aaf55ae519c..d46233fc7ec923dc8908c5581d6bdb228551443f 100644
--- a/cjs/src/connection.js
+++ b/cjs/src/connection.js
@@ -173,8 +173,10 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       return write(toBuffer(q))
         && !q.describeFirst
         && !q.cursorFn
-        && sent.length < max_pipeline
+        // suzu.one patch: onexecute must run whenever the query was written, even with pipelining
+        // off (max_pipeline: 0); begin() and reserve() rely on it to claim the connection.
         && (!q.options.onexecute || q.options.onexecute(connection))
+        && sent.length < max_pipeline
     } catch (error) {
       sent.length === 0 && write(Sync)
       errored(error)
diff --git a/src/connection.js b/src/connection.js
index 1b1cccde43b4570d5d071d6ffaab2669b3c2065a..81b6b48949d54296c7c9abd4870b270d1cd3e274 100644
--- a/src/connection.js
+++ b/src/connection.js
@@ -173,8 +173,10 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       return write(toBuffer(q))
         && !q.describeFirst
         && !q.cursorFn
-        && sent.length < max_pipeline
+        // suzu.one patch: onexecute must run whenever the query was written, even with pipelining
+        // off (max_pipeline: 0); begin() and reserve() rely on it to claim the connection.
         && (!q.options.onexecute || q.options.onexecute(connection))
+        && sent.length < max_pipeline
     } catch (error) {
       sent.length === 0 && write(Sync)
       errored(error)
